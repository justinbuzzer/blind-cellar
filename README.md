# Blind Cellar

Private blind tasting, fairly scored. Every participant — including the host — privately registers their own bottle(s), everyone joins from their own phones and guesses blind, and once revealed the app scores everyone and shows a report with each bottle's contributor.

## Running it

1. Set up Supabase — see [SUPABASE_SETUP.md](SUPABASE_SETUP.md) (create a project, run `supabase/schema.sql`, get your URL + anon key).
2. Copy `.env.example` to `.env.local` and fill in the two Supabase values.
3. Install and run:

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000). A host creates a tasting on one device; everyone — including the host — registers their own bottle(s) from their own phones, then joins the blind tasting via a shared link, QR code, or join code.

Other useful commands:

```bash
npm run build     # production build
npm run lint      # ESLint
npm run test      # unit tests (Vitest)
npx tsc --noEmit  # type-check
```

## What this app supports

A tasting session moves through three stages: **registration** → **collecting** → **revealed**.

- **Host setup**: the host enters a title, date, their own display name, and a required **tasting format** (see "Tasting modes" below) — no wines up front. This creates the session and a normal participant record for the host — the host is a participant like anyone else, and appears exactly once in the leaderboard.
- **Registration**: the host gets a management link (`/host/[publicId]?token=...`) with a QR code, join link/code, live bottle count, and a "Start tasting" action. Every participant — including the host, via "Register my bottle" — can privately register any number of bottles using controlled dropdowns for country and region (region filtered by country), a grape/blend selector for single variety **or a structured multi-select blend picker** (see "Structured grape/blend" below), a year picker or NV toggle for vintage, a required **wine style** (Bubbles/White/Red/Sweet/Other), and free-text producer/wine-cuvée/optional private note. Each bottle gets a permanent, sequential anonymous number (`Bottle 1`, `Bottle 2`, …) the moment it's registered; numbers are never reused, even after a delete. The host can also arrange a separate **tasting order** for the bottles (see "Bottle number vs. tasting order" below) from a "Tasting order" panel on the host page, using Move up/down controls; this locks the moment tasting starts. Participants can edit or delete only their own bottles, and only during registration. No one — including the host — can see another contributor's bottle details or identity at this stage; the host sees only each bottle's anonymous number, wine style, and tasting-order position, never country/region/producer/vintage/notes or who contributed it.
- **Collecting**: the host starts tasting once at least one bottle is registered; this locks bottle registration, numbering, and the tasting order. What happens next depends on the session's **tasting mode** — see "Tasting modes" below.
- **Revealed**: whether reached via full blind's one-shot reveal, course-by-course's final bottle, or seen's "End tasting" action, the shared report unlocks for everyone in real time. For full blind and course-by-course, the report shows each bottle's full answer key, its **wine style** and **served position** (e.g. "Style: Red · Served 1st"), **and its contributor's name**, alongside Wine of the Night, Best Taster, Most Divisive Wine (with tie handling), per-bottle guess breakdowns (core vs. bonus categories), and the taster leaderboard. Seen tasting's report is rating-only instead — see "Seen tasting" below.
- **Scoring** is a 120-point-per-wine model: a 100-point **core** score (country 20, region 30, grape/blend 30, vintage 20 — exact, case/whitespace/accent-lenient matching) plus a 20-point **bonus** score (producer 10, wine/cuvée 10, exact normalised match). Price band is never scored, and no points are awarded or deducted for contributing a bottle. The leaderboard ranks by total points, then core points, then exact core-category matches, with ties sharing a rank. See "Scoring model" below for full details.
- A **"See a demo report"** link on the home page (`/demo`) renders a canned 3-bottle, 3-taster report (including contributor names) entirely client-side, for a quick look without creating a real tasting. It's explicitly labeled as a local-only demo and isn't saved anywhere.

## Tasting modes

Chosen once by the host when creating a session (`tasting_sessions.tasting_mode`) and **never editable afterwards**. Every session created before this feature existed safely reads as `full_blind` — today's only behaviour.

### Full blind tasting (default)

> All bottles are tasted blind before any wines are revealed. Best for comparative tastings where complete objectivity matters.

The existing, original flow: every participant (including the host and a bottle's own contributor) guesses every bottle **in the host's tasting order** (progress shown as e.g. `Bottle 4 — 1 of 5`) at their own pace via Previous/Next, autosaved as they go. No bottle's answer key, contributor identity, other guesses, or score is visible to anyone until the host performs one final reveal — there is no per-bottle reveal control in this mode. The host control page shows "All bottles remain hidden until the final reveal." during collecting.

### Course-by-course reveal

> Each bottle is tasted blind, then revealed before moving to the next. Best for casual dinners and relaxed tasting discussions.

Only one **active bottle** is open at a time — the earliest bottle by tasting order that the host hasn't yet revealed:

- Participants can only see and guess the active bottle (`/session/[publicId]/active`) — never an upcoming one's identity, guesses, or scores.
- A participant finalises their guess by tapping **Lock in my guess** (requires a rating, same as full blind's final-submission bar); the form then shows *"Your guess for Bottle [number] is locked. Waiting for the host to reveal it."* Unlike full blind, this locks per-bottle, not the whole session.
- The host doesn't need everyone to submit — **Reveal Bottle [number]** is available at any time, with a confirmation warning that anyone who hasn't submitted gets no score for that bottle (no guess is fabricated on their behalf). The host also sees a live aggregate `N of M participants submitted` count for the active bottle only, never individual guesses.
- Revealing shows a focused per-bottle screen (`/session/[publicId]/bottle/[wineId]/reveal`): full answer key, contributor, group rating stats, and every participant's guess/score breakdown for *that bottle only* — reusing the exact same scoring functions the final report uses, just scoped to one wine. There is deliberately no running leaderboard here, to keep a mid-tasting screen simple.
- A participant chooses **Continue to next bottle** whenever they're ready (never force-navigated away from a reveal they're still reading); reloading or returning later routes them to whatever's now current — the next active bottle, or final results if the session finished revealing while they were away.
- The host cannot skip a bottle, reveal out of order, reveal the same bottle twice, or reveal the whole session in one step — only the current active bottle, one at a time. Revealing the last bottle transitions the session straight to `revealed` and the normal final report.
- Bottle numbers and tasting order are never touched by any of this — only a new `wines.revealed_at` timestamp (null = not yet revealed) and a new `wine_guesses.locked_at` timestamp (null = still an editable draft) change.

### Seen tasting

> All bottles are visible from the start. Best for relaxed tastings where guests want to compare wines openly and rate them at their own pace.

A tasting-note-and-rating format, not a blind-identification competition — there is no guessing, no scoring, and no Best Taster:

- The moment the host starts tasting, **every bottle's full details become visible to every participant** — style, country, region, grape/blend, vintage, producer, wine/cuvée, and contributor name — at `/session/[publicId]/seen`. Bottle registration's usual pre-start secrecy (no contributor sees another's bottle) is unaffected; visibility only opens up once collecting begins.
- Participants **rate any bottle in any order** — tasting order is display order only, not a pacing restriction — and can **revisit and change a saved rating as many times as they like** while the session stays `collecting`. There is no per-bottle lock and no session-wide "submitted" state; a rating is simply the latest value saved for that `(guest, bottle)` pair.
- All individual ratings (and optional notes) stay private between participants during collecting — the host sees only aggregate progress (`N of M participants have rated at least one bottle`, `N of M possible ratings entered`), never anyone's individual rating.
- The host ends the tasting with **End tasting and reveal ratings**, which locks every rating (further save attempts are rejected server-side) and reveals a rating-focused report: Wine of the Night and Most Divisive Wine by average rating, a full bottle ranking (average → lower spread → more ratings → shared tie), and — expandable per bottle — every participant's rating (`No rating` shown honestly for anyone who skipped a bottle, never a fabricated zero).
- A seen rating is stored in the same `wine_guesses` table as full_blind/course_reveal guesses, but only ever sets `rating`/`confidence`/`tasting_note` — every identification-guess column simply keeps its existing blank default, exactly as an untouched draft guess already does in the other two modes. See `upsert_seen_rating` in `supabase/schema.sql`.

### Seen Host Controls

Host Controls for a Seen tasting shows the actual wine, not a generic bottle label — since Seen identity is already visible to every participant the moment collecting starts (see above), there's no secrecy reason to hide it from the host either:

- Each registered bottle shows **`Producer — Wine/cuvée`** as its primary line, a secondary metadata line (`Vintage/NV · Appellation, Region · Country`, omitting missing values and never repeating an Appellation that equals its Region), and the bottle's existing anonymous label (e.g. `Bottle 3`) kept only as small operational context — see `formatSeenWineIdentity`/`formatSeenWineSecondaryLine` in `lib/seenHostControls.ts`.
- Each bottle shows its own rating-response count, **`{ratedCount} of {eligibleCount} rated`** — `ratedCount` is eligible participants with a valid submitted rating for that specific bottle, `eligibleCount` is every guest in the session (one consistent denominator for every bottle, matching the session-wide progress this mode already tracked).
- The host can **reveal the group rating for any one bottle independently, in any order, without ending the tasting** — `Reveal ratings` opens a short confirmation (`Reveal ratings for this wine?` / `Participants will be able to see the group rating for this wine.`) and calls `reveal_seen_ratings`, which stamps that bottle's `wines.ratings_revealed_at` (idempotent — revealing an already-revealed bottle is a no-op success, never an error). This is a new, separate timestamp from course_reveal's `revealed_at`: in Seen mode the wine's *identity* is never hidden, so this column only ever gates the *rating aggregate*, and it is never read or written by full_blind/course_reveal, which have no equivalent per-bottle rating-reveal concept at all.
- Once revealed, the host sees **`Ratings revealed`**, the group average (`Group rating: 92.4`, or `No ratings submitted` if nobody rated it, one decimal place, computed from valid submitted ratings only), and the same `{ratedCount} of {eligibleCount} rated` line. Every other bottle stays exactly as it was — revealing one bottle never touches any other bottle's state.
- **Revealing a bottle's ratings locks it for participants**: `upsert_seen_rating` rejects any further insert/update for that specific bottle once its `ratings_revealed_at` is set (`ratings_already_revealed`), so the average a host and participants have already seen can never silently drift. Every other, still-unrevealed bottle keeps its existing free-to-revise-until-the-tasting-ends behaviour untouched. This lock is scoped to Seen mode only — full_blind's guess-lock and course_reveal's `lock_wine_guess` are separate, unrelated mechanisms this feature never touches.
- Participants see a revealed bottle's group rating (and nothing else new — never a rated/eligible count, never another participant's individual rating) the next time their own Seen bottle list or rating-entry page refreshes, via the existing `get_seen_tasting_state` poll — no new realtime channel was added. An unrevealed bottle's aggregate is never present in that response at all, regardless of how many participants have already rated it.
- **Ending the tasting** (`End tasting and reveal ratings` / `end_seen_tasting`) is completely unchanged — it still works in one step, session-wide, regardless of which individual bottles the host did or didn't reveal beforehand, and the final report (built from the reveal-gated `guest_visible_wines`/`revealed_wine_guesses` views, only once `status = 'revealed'`) always shows every bottle's full result set, exactly as before this feature.

### Host per-bottle response progress

A small, host-only icon on each eligible bottle row in Host Controls opens a temporary popover showing how many eligible participants have submitted the relevant response for that one bottle, and the safe display names of the ones who haven't — a pouring/pacing aid, never a content reveal.

- **Full blind**: the icon appears on every bottle. Because full_blind has no per-bottle lock (only the session-wide final submit), the count is the same for every bottle in a session — submitted = `guests.completed_at is not null`, the identical signal `complete_guest_submission` itself uses to gate a final submit, and the same one already shown per-participant in the existing "Submitted / In progress" list.
- **Course-by-course**: the icon appears only on the current active bottle's row — never on a not-yet-released or already-revealed bottle, even via a manipulated request, since the server independently re-derives "the active bottle" (same lookup `lock_wine_guess` uses) and rejects anything else with the same `bottle_not_active` error used elsewhere. Submitted = `wine_guesses.locked_at is not null` for that bottle, matching the existing "N of M submitted" active-bottle card.
- **Seen**: the icon appears on every bottle, independent of whether its group rating has been revealed — this is a distinct, coexisting affordance from the existing `{ratedCount} of {eligibleCount} rated` line and `Reveal ratings` control; opening it never changes reveal state. Submitted = `wine_guesses.rating is not null` for that bottle, matching the existing `ratedCount`.
- **Eligible participant** is the same `guests` row count used throughout this file for every mode — the host is never counted separately or twice, since they only ever have the one `guests` row created at session creation (see `create_tasting_session`).
- The popover never shows a guess, a draft, a rating value, a confidence, a note, a score, a group average, an email, or a token — only the two counts and the existing safe `guests.display_name` of whoever hasn't submitted yet. See `get_bottle_response_progress` in `supabase/schema.sql` and `lib/hostProgress.ts` for the exact formatting.
- Computed entirely from existing `guests`/`wine_guesses`/`wines` data on demand (only when the host opens the popover) — no new persisted progress table, and no change to any existing reveal, lock, or scoring behaviour.

### Host guess screen group progress

While the host is entering their own Full blind or Course-by-course guess, their own guess screen (`/tasting/[publicId]` or `/session/[publicId]/active`) shows a small, host-only live line above the guess form:

```text
Your private guess
Group progress · 6 of 8 submitted
```

- Shown **only** to the session's own host, guessing as a participant — never to an ordinary participant, and never on a Seen rating page. The host-only check is server-side (`get_host_guess_progress` independently re-verifies the calling guest token belongs to `tasting_sessions.host_guest_id`); the client's local host-token check ([`getHostToken`](lib/deviceStorage.ts), the same one `HostControlsLink` already uses) only decides whether to attempt the fetch, never whether it succeeds.
- Uses the **exact same eligible/submitted definitions** as the Host Controls popover above (full_blind: `guests.completed_at`; course_reveal: `wine_guesses.locked_at` for the current active bottle only) — see `get_host_guess_progress` in `supabase/schema.sql`, deliberately mirroring `get_bottle_response_progress`'s predicates so the two can never quietly diverge.
- Returns **only** `{submittedCount, eligibleCount}` — a stricter, separate DTO than the Host Controls popover's: no missing-participant names, no bottle id, no response kind. The host guess screen can never learn who has or hasn't submitted, only the aggregate.
- **Course-by-course**: scoped to the current active bottle only, via the same server-side active-bottle guard used everywhere else in this feature — a not-yet-released or already-revealed bottle can never be queried, so the host's own guess screen never leaks anything about a future bottle.
- **Full blind**: the count is session-wide (identical for every bottle, since full_blind has no per-bottle lock), fetched once and kept live via a realtime subscription on `guests` (already realtime-enabled) — Previous/Next never needs to re-fetch it.
- No scoring, reveal, lock, or Course release behaviour changed by this feature — it is a read-only aggregate display.

## Results reveal

Host-controlled, per-bottle results reveal for Full blind and Course-by-course tastings. Bottle numbers and contributor names are never secret (see "Bottle-order contributor labels" above); only actual wine identity and scores are gated behind an explicit host reveal action.

- **Full blind**: the host can reveal any bottle, at any time, **in any order** — a brand-new `reveal_full_blind_bottle` RPC, deliberately without course_reveal's "must be the earliest unrevealed bottle" check. Host Controls shows one row per bottle (Unrevealed/Revealed) with a `Reveal results`/`View results` action per row, mirroring the existing Seen per-bottle list. Revealing the very last bottle automatically flips `tasting_sessions.status` to `'revealed'` — the same tail behaviour `reveal_bottle` already has for course_reveal — which is what makes the existing, unmodified `/results/[publicId]` final report/leaderboard page become reachable the moment every bottle has been revealed, with no new final-leaderboard route needed.
- **Course-by-course**: reveal order is completely unchanged — `reveal_bottle`'s existing "must be the current active bottle" rule is untouched. What's new is that a host can now **revisit** an already-revealed bottle's results from Host Controls (a "Revealed bottles" list), a capability that didn't exist before.
- **Seen tastings are explicitly out of scope** — Seen already has its own per-bottle rating-reveal model (`reveal_seen_ratings`), which this feature does not touch, extend, or duplicate.

**Participant access**: a participant can only ever see a bottle's actual identity and their own score once that bottle's `revealed_at` is set — enforced server-side by `get_revealed_bottle`, never by a client-side check. That RPC now returns **only the caller's own guess** (0 or 1 elements), a privacy tightening from its previous behaviour of returning every guest's guess to any caller. A new participant "results hub" (`/tasting/[publicId]/results` for full_blind, `/session/[publicId]/results` for course_reveal, via `get_revealed_bottles_summary`) lists every bottle's safe number/label and reveal state, links to whichever bottles are already revealed, and shows the final leaderboard link only once every bottle has been revealed — otherwise it shows "The final leaderboard will be available after all results are revealed." with no rank/total/count leaked. Full blind's new per-bottle result page lives at `/tasting/[publicId]/bottle/[wineId]/result`; course_reveal reuses its existing `/session/[publicId]/bottle/[wineId]/reveal` screen, tightened to show only the caller's own score breakdown (the "Everyone's guesses" list was removed).

**Host access**: a new `get_bottle_result_for_host` RPC (host-token authenticated) returns every eligible participant's submission status and guess for one already-revealed bottle — never before reveal. Host Controls links to a per-bottle result page (`/host/[publicId]/bottle/[wineId]/result`) showing the actual wine identity, an aggregate score summary (submitted count, average score, highest score — all in the bottle's own native denominator, since every guesser of one bottle shares an identical possible-points total), and a per-participant score breakdown (collapsible rows; a participant with no submitted guess shows "No submitted guess for this bottle." rather than being silently dropped).

**Provisional leaderboard** (host-only, `/host/[publicId]/leaderboard`, via a new `get_provisional_leaderboard_for_host` RPC): ranks participants across **currently-revealed bottles only**, labelled "Provisional leaderboard — revealed bottles only" until every bottle has been revealed. No new ranking math exists for this — it constructs a synthetic `TastingSession` scoped to just the revealed wines and calls the exact same `buildRevealedSubmissions`/`buildCourseRevealSubmissions` + `calculateTasterResults` pipeline the final `/results` report already uses (see `lib/resultsReveal.ts`), so percentage-primary ranking, mixed-80/100-denominator handling, and tie-break order (`overallAccuracyPercent` → `totalPoints` → `submittedGuessCount` → `exactCoreMatches`) are all inherited unchanged. A genuine remaining tie displays alphabetically by display name (submissions are pre-sorted alphabetically before ranking, relying on `Array.sort`'s stability) rather than inventing a new tie-break the existing data can't support.

**Established codebase-wide scoring pattern, followed rather than replaced**: every RPC in this feature returns server-authorized-but-raw wine/guess data (gated on `revealed_at`, tasting mode, and host/guest token authorization in SQL) — never a pre-computed score. All score/rank math still runs client-side via the same shared pure functions (`lib/scoring.ts`, `lib/results.ts`) every other result surface in this app already uses. This matches `buildRevealedBottleResult` and `loadTastingReportData`'s existing, established design: the *data access* is server-gated, even though the *arithmetic* runs in the browser.

**Privacy**: no participant can ever see another participant's guess, score breakdown, or submission status. No unrevealed bottle's identity, score, or guesses are exposed through any route, payload, error message, or prefetch. Reveal actions are idempotent (`coalesce(revealed_at, now())`) and host-only; there is no "unreveal" action. `wine_guesses` remains outside Realtime and the anon grant, exactly as before — reveal-adjacent live updates use the same realtime-signal-plus-poll-fallback convention already established for `wines`/`tasting_sessions`.

## Scoring model

**Every session is permanently tagged, at creation, with the scoring model it uses** (`tasting_sessions.scoring_version`) — chosen entirely server-side, never a client-supplied value, and never editable afterwards. Reports are still recalculated from live data on every load (there's no separate stored/frozen score row), but the calculation itself now branches on the session's own `scoring_version`, so revealing an old session always reproduces that session's original scoring rules, forever — never the current ones. Only two versions have ever actually existed:

- **`legacy_v1`** — every session created before this scoring-model replacement. Described below under "legacy_v1 (historic sessions)".
- **`core_v3_appellation_conditional`** — the current model, described below under "core_v3_appellation_conditional (current)". Every newly created session gets this version.

There is no intermediate version: an earlier plan to add a 140-point Appellation-bonus tier was never actually implemented (Appellation guesses existed but were always explicitly unscored), so `legacy_v1` is the only historic model that needs preserving.

### core_v3_appellation_conditional (current)

Five potential 20-point core categories, no bonus category at all:

| Category | Weight |
|---|---|
| Country | 20 |
| Region | 20 |
| Appellation | 20 — only when the actual wine has one recorded |
| Grape / blend | 20 |
| Vintage | 20 |

- **Conditional Appellation, conditional denominator.** If the actual revealed wine has a non-null Appellation, the possible score for that bottle is **100** (all five categories); if it doesn't, Appellation is excluded entirely — from the guess's field list, from the score, and from the denominator — and the possible score for that bottle is **80**. A participant is never penalised, and never scores an accidental correct, for a category that didn't exist for that particular wine — a blank Appellation guess against a wine with no Appellation is simply not part of the calculation, not a wrong answer.
- **Appellation correctness** is a plain exact-match comparison (same normalisation as every other field — trim, case-insensitive, accent/punctuation-lenient) between the guess and the actual Appellation, evaluated completely independently of Country/Region/Grape/Vintage — a correct Appellation call earns its 20 points even if every other field is wrong, and vice versa. No partial matches, no inference from other fields.
- **Producer and wine/cuvée are never scored** under this model — there is no bonus category. They remain optional guess fields, labelled `Producer, optional` / `Wine / cuvée, optional` in the guess form, and still appear in the reveal/report as a plain, unscored "Personal precision calls" comparison (`Guess: … / Wine: …`) if either side has a value — exactly the same non-scored comparison treatment `legacy_v1` already gave Appellation guesses.
- **Country, Region, Grape/blend, Vintage** use exactly the same comparison logic as `legacy_v1` (see below) — only the point values (20 instead of 20/20/30/20 — Region and Grape/blend dropped from 30 to 20) and the conditional Appellation category are new.
- **Ranking is by percentage, not raw points**, precisely because bottles can be scored out of either 80 or 100: `sum(points earned) / sum(possible points for the bottles this taster actually submitted a guess for) * 100`. A bottle the taster never guessed on is excluded from both sums, so someone who only tasted 80-point wines is never unfairly compared against someone who tasted 100-point wines. Ties break on: raw points earned, then number of submitted guesses, then count of exact core-category matches (the same final tie-break `legacy_v1` uses). The report displays both the percentage and the raw score in explanatory form (e.g. `284 / 360 points`).
- **Best Taster** is the taster ranked #1 by this same percentage-first ranking.

### legacy_v1 (historic sessions)

Frozen forever for every session created before this feature. Each guess earns a **core score (0–100)** and a **bonus score (0–20)**, for a **total (0–120)** per wine:

| Category | Weight | Type |
|---|---|---|
| Country | 20 | Core |
| Region | 30 | Core |
| Grape / blend | 30 | Core |
| Vintage | 20 | Core |
| Producer | 10 | Bonus |
| Wine / cuvée | 10 | Bonus |

- **Country, region, vintage** and **producer, wine/cuvée**: exact match after case/whitespace/accent/punctuation-lenient normalisation (`lib/normalize.ts`). No partial credit.
- **Grape / blend**: a **single variety** guess must exactly match the answer's canonical grape (aliases like Syrah/Shiraz, Pinot Gris/Pinot Grigio, and Zinfandel/Primitivo count as equal). A **blend** guess is tokenised on commas/slashes/semicolons/ampersands/hyphens, each token alias-canonicalised, and compared as an unordered set against the answer's blend — an exact set match is required, no partial-overlap credit. If the guess's mode (single/blend) doesn't match the answer's mode, it scores zero even if the text overlaps. If either side's mode is unknown (legacy data predating this field), scoring falls back to a plain alias-aware text comparison.
- **Vintage**: `NV` is an exact value, not a year — it only matches another `NV`, never a specific year.
- **Appellation guesses are never scored** under this model — see README "Region and Appellation" — "Blind-guess Appellation" for the unscored guess-vs-actual comparison shown in the reveal/report instead.
- **Confidence** is captured but never scored. **Price band** is never scored (and no longer collected in bottle/guess forms — see "Data model notes" below).
- The taster leaderboard ranks by **total points**, then core points, then count of exact core-category matches; a full tie on all three shares a rank — this ranking rule never changes for `legacy_v1` sessions, even after the ranking rule above was introduced for new sessions.

### Shared machinery

Both models share the same normalisation (`lib/normalize.ts`), the same grape/blend comparison engine (`lib/scoring.ts`'s `scoreGrapeBlend`, parameterised by point value), and the same curated Appellation map (`lib/appellations.ts`) — nothing about matching logic was duplicated between them, only the point weights, category list, and ranking rule differ. `lib/scoring.ts` keeps the two models' calculations in clearly separate, independently named functions (`scoreWineGuessLegacyV1` / `scoreWineGuessCoreV3`) behind one dispatcher (`scoreWineGuess`) that selects by the session's `scoringVersion` — never a shared, branchy implementation.

## Structured grape/blend

Choosing **Blend** replaces free-text entry with:

- **Grapes in this blend** — a multi-select of the same curated list single-variety mode uses (search box + checkboxes + removable chips), so blend components are canonical and score-comparable the same way a single variety is, regardless of typing/spelling/ordering differences between contributor and guesser.
- **Other grape(s), if not listed** — an optional free-text field for varieties not on the curated list (e.g. "Carignan, Counoise"). Multiple entries are split on commas/slashes/semicolons/ampersands/line breaks, trimmed, deduplicated case-insensitively, and alias-resolved to a curated grape's canonical name where recognised (so typing "Shiraz" here is the same as checking Syrah above) — a grape already picked from the curated list can't also sneak in via this field.

At least two total grapes (curated picks + unlisted entries, after the deduplication above) are required to save a blend on a bottle; one selected grape shows *"Select at least two grapes for a blend, or choose Single variety instead."* rather than saving. Guess entry stays lenient for autosave (a blank/incomplete draft is fine — see "Scoring model" above for why a blank guess just scores zero), but the same message blocks *final* submission if a guess's blend has exactly one grape, mirroring the existing missing-rating check.

Internally, the two curated/free-text inputs are combined into one canonical, alphabetised list (`lib/wineReferenceData.ts`'s `combineBlendComponents`) that becomes the "/"-joined display/storage text (e.g. `Cabernet Sauvignon / Merlot / Carignan`) — the same shape a blend has always been stored in, so scoring needed no changes (see "Scoring model" above). A new nullable `wines.grape_blend_components` / `wine_guesses.grape_blend_components` JSONB column separately stores which parts came from the curated multi-select vs. the free-text field (`{"selectedGrapes": [...], "otherGrapesText": "..."}`), purely so a blend can be re-opened for editing without losing that distinction — it is never used for scoring or display, and is never exposed to anon directly or through any pre-reveal view (only through the existing token-gated `get_registration_state`/`get_guest_session_state` RPCs, scoped to the caller's own bottles/guesses). A blend saved before this feature (no structured record) is re-parsed from its flattened text when its edit form loads — grapes matching the curated list get pre-checked, everything else lands in the free-text field verbatim — without ever needing to be re-saved.

## Other grape (single-variety custom entry)

**Single variety** mode's dropdown has one final option, **"Other grape"**, for a variety not on the curated list. Selecting it reveals a plain text input, labelled **"Grape variety"** with helper text **"Enter the grape variety."**, directly below the dropdown.

- **Available only for single-variety entry.** Blend mode already has its own unrelated free-text path ("Other grape(s), if not listed" — see "Structured grape/blend" above); this feature does not touch it, and a custom single grape can never masquerade as a blend or vice versa.
- **Stores the entered name in the existing grape value field — no new database column.** The typed text is saved directly as the bottle's/guess's canonical single-variety value (the same `grape_style`/`grape_style_guess` column every other single-variety grape already uses); the literal label "Other grape" is never itself persisted as a grape value. Which control to show (dropdown vs. text input) is tracked by an `otherGrapeSelected` flag that exists only in client/React form state — it is never sent to the server and never stored.
- **Does not create a separate stored sentinel/value.** Reopening a bottle or guess draft whose stored grape isn't on the curated list automatically re-selects "Other grape" and repopulates the text field with the stored value (`isCustomSingleGrape` in `lib/wineReferenceData.ts`, shared by every place that loads a record into editable form state) — nothing is silently blanked or nudged to the nearest curated grape.
- **Validation** (`lib/validation.ts`): required, trimmed, max 80 characters, letters (including accented Latin)/spaces/hyphens/apostrophes only. A comma/slash/ampersand/semicolon is rejected with *"Use Blend for more than one grape."* rather than the generic invalid message, so a contributor who types "Cabernet Sauvignon / Merlot" here is redirected to Blend mode instead of silently truncated or split. Guess entry stays lenient for autosave — a blank Other-grape draft is fine, same leniency as any other unset guess field — but a *non-blank, invalid* custom grape guess blocks final submission/lock, mirroring the existing incomplete-blend check.
- **Scoring uses the existing grape/blend comparison logic unchanged.** `scoreGrapeBlend` already falls back to plain alias-aware text comparison for any grape it doesn't recognise, so two matching custom grapes (e.g. both "Mondeuse Blanche") score correct exactly like a curated match, and two different or mismatched custom grapes score zero — no new "unknown grape" rule, no free credit, no scoring-model or point-weight changes.

## Grape-entry assistance

A curated, intentionally incomplete v1 shortcut that pre-fills a bottle's grape/blend from its Country + Region + Appellation + Wine style, and filters the single-variety dropdown by skin colour — active everywhere the shared grape/blend picker is (participant/host bottle registration, editing an available/eligible bottle, Cellar Add/Edit, Full blind guess entry, Course-by-course guess entry, and draft restore/reopen for all of them). It is an entry-speed convenience only: every value it sets is stored and validated exactly like a manually typed one, and it never changes scoring, Appellation applicability, or the 80/100-point denominator.

- **White/Red filter the single-variety dropdown by skin colour** (`lib/wineReferenceData.ts`'s `GrapeVarietyOption.skin`, `singleGrapeVarietyOptionsForStyle`) — White shows only white-skinned curated grapes, Red only red-skinned ones, every other style (Bubbles/Sweet/Other/no style chosen yet) shows both, since those categories can legitimately be either colour. **Other grape** is always the final dropdown option regardless of style — it's a UI fallback for a variety not on the curated list, never itself classified by colour, so a custom entry can never be auto-cleared for a colour mismatch. Changing style to one that makes the current curated single-grape selection the wrong colour (e.g. Chardonnay, then switching White → Red) clears the grape selection with the accessible announcement *"Grape selection cleared because the wine style changed."* rather than silently substituting a same-colour grape; a Blend selection is never auto-cleared or mutated just because style changed.
- **One central mapping key — Country + Region + Appellation + Wine style** (`lib/grapeAssistance.ts`'s `getGrapeAssistance`) — looked up most-specific first: (1) Country+Region+Appellation+Style, (2) Country+Region+Appellation, (3) Country+Region+Style, (4) Country+Region, else no automatic selection. There is deliberately no country-only tier, and an appellation-/style-specific tier is only ever consulted when that field is actually non-blank — leaving Appellation blank never accidentally matches an appellation-specific entry. A curated single-variety match switches to Single variety mode and selects exactly one grape; a curated blend match switches to Blend mode and preselects the mapping's component grapes — both remain fully editable afterward (another dropdown pick, Other grape, switching mode, or freely editing blend components all work exactly as before this feature).
- **No confidence tiers, no confirmation dialog.** There are only three outcomes — a single-grape match, a blend match, or no automatic selection — never "certain"/"likely"/"suggested"/"AI selected" labelling, and an auto-applied value can be submitted or edited immediately with no extra confirmation step. The only feedback is a small, transient inline line under Grape/blend reading *"Set from region and appellation. You can edit it."*, shown only right after an automatic selection is applied — never a persistent banner, never shown when nothing was auto-applied.
- **Manual edits are never overwritten** (`lib/useGrapeAssistance.ts`, `lib/grapeAssistance.ts`'s `evaluateGrapeAssistanceChange`): each form tracks, in local component state only (never persisted, never sent to the server), whether the current grape value is `empty` (nothing entered yet — eligible for auto-fill), `auto` (this feature set it last, and hasn't been touched since — eligible to be *recomputed* on a further location/style change, including clearing if no mapping applies anymore or switching between single/blend as the match itself changes kind), or `manual` (the contributor/guesser picked or typed it themselves — never auto-mutated again, except that a colour-incompatible curated single grape is still cleared on a style change, since an invalid Red-style-with-a-white-grape combination is worse than losing an unrelated auto-fill). **A record or draft loaded with an existing grape value always starts as `manual`** — opening an existing bottle/cellar entry/guess draft never overwrites what's already saved, regardless of what the current mapping would suggest.
- **Blind guess forms use the registered bottle's actual style as a private, non-displayed hint** (Full blind and Course-by-course only — see "Privacy-safe blind-guess style hint" below). A taster never sees or guesses a wine-style field (`types/tasting.ts`'s `WineGuess` has none), but the bottle they're guessing does have one; the server derives a minimal, one-way `styleHint` (`white_skin_only` / `red_skin_only` / `all_skins`) from it and includes that — never the raw style — in `get_guest_session_state`/`get_active_bottle_state`'s existing per-bottle response. `lib/grapeAssistance.ts`'s `styleFilterKeyForHint` converts the hint back to the same `"white"`/`"red"`/`""` vocabulary the filtering and mapping-lookup code above already accepts, so this reuses those exact code paths with no second grape mapping, no second colour classification, and no new participant-visible control. The mapping-lookup style input is therefore private and server-derived in blind forms, but always the taster's own guessed Country/Region/Appellation.
- **What this does not do**: it is a curated shortcut for well-known, largely monovarietal or classically-blended appellations — not a comprehensive wine-law database. An unlisted appellation/region (e.g. Alsace, Pfalz, Wachau, Riverina, Menfi) simply has no automatic selection, by design, and nothing about this feature adds, removes, or renames a Country/Region/Appellation value (the curated Appellation dropdown data is read-only input to this feature, never modified by it).

### Privacy-safe blind-guess style hint

Extends the feature above into Full blind and Course-by-course guess entry only (Seen tasting, bottle registration, cellar forms, and host controls are untouched — see README "Tasting modes").

- **The raw actual wine style never reaches a blind-guess client.** `supabase/schema.sql`'s `wine_style_grape_options_hint(wine_style)` — a plain (non-`SECURITY DEFINER`) SQL helper, not granted to any role, callable only from inside another already-privileged function — maps `white`→`white_skin_only`, `red`→`red_skin_only`, and every other/unknown value (Bubbles, Sweet, Other, legacy null) → `all_skins`. `get_guest_session_state`'s `wines` array and `get_active_bottle_state`'s `activeBottle` object each gain one new field, `styleHint`, computed this way — both RPCs already return only the calling guest's own token-scoped bottles, so no new authorization surface is added, and no generic "look up any bottle's hint" endpoint exists.
- **The hint only ever does two things client-side**: filters the single-grape dropdown (identically to White/Red filtering elsewhere — see above) via `styleFilterKeyForHint`, and feeds the exact same `wineStyle` parameter into `getGrapeAssistance`'s existing Country+Region+Appellation+Style lookup, so a guessed Bordeaux under a Red-hinted bottle auto-suggests the red Bordeaux blend while a White-hinted one suggests the white blend — without either the hint or the resulting rationale ever being displayed, labelled, or distinguishable from an ordinary auto-fill. `all_skins` behaves exactly like "no style" already does elsewhere (shows every grape; only Country+Region+Appellation tiers can match) — Bubbles/Sweet/Other/Rosé/unknown actual styles are therefore indistinguishable from each other for assistance purposes, a deliberate, minor precision trade-off for using the same three-value hint everywhere rather than adding a second, wider channel.
- **A participant's own prior guess is never silently discarded.** Since `styleHint` is fixed for a bottle's entire guess-entry lifetime (there is no participant style control to change it), the existing style-change colour-clearing rule can structurally never fire in a blind-guess form. `lib/wineReferenceData.ts`'s `singleGrapeVarietyOptionsPreservingCurrent` additionally guarantees that if a restored draft's grape is the "wrong" colour for the hint, it still renders as the selected `<select>` option (prepended ahead of the filtered list) rather than silently blanking — with no visible explanation of why it sits outside the normal set.
- **Nothing about this is persisted or leaked**: `styleHint` lives only in the two read RPCs' response shape; `upsert_wine_guess`/`wine_guesses` never gained a column for it, no draft/guess payload includes it, and it is never present in a URL, log line, or error message.

### Bottle-order contributor labels

Extends Full blind and Course-by-course guess entry only (Seen tasting, host controls, cellar, registration, results/reports, public pages, and the wine-identity reveal cards are untouched) with a pouring/navigation coordination cue: the plain `Bottle N` label used throughout those two flows' bottle-order UI (current-bottle heading/progress bar, the WineGuessForm's bottle-code display, and course-reveal's locked-guess eyebrow label) now appends the contributor's existing session display name, e.g. `Bottle 2 — Sarah`. This is a coordination cue for pouring order only — it never reveals producer, wine/cuvée, country, region, appellation, grape/blend, vintage, wine style, or any other actual wine-identity detail before the existing reveal rules permit it.

- **Reuses the existing, already-safe `guests.display_name` exposure** — the same session-scoped contributor name already shown to every participant in Seen mode (`get_seen_tasting_state`) and in post-reveal per-bottle DTOs (`get_revealed_bottle`), via the same `wines.contributor_guest_id → guests.display_name` subselect. No new name field, no account/profile/email data, and no new grant on the `guests` table (the subselect runs inside the existing `SECURITY DEFINER` RPCs, exactly like `wine_style_grape_options_hint` above). `get_guest_session_state`'s `wines` array and `get_active_bottle_state`'s `activeBottle` object each gain one new field, `contributorName: string | null` — null when the bottle has no recorded contributor.
- **`lib/codes.ts`'s `formatBlindBottleLabel(bottleNumber, contributorDisplayName)`** is the single place this formatting happens — built on the existing `bottleLabel` helper so the two can never drift. A missing, empty, or whitespace-only name falls back to the plain `Bottle N` label (never "Unknown", "Anonymous", a dangling em dash, or an internal identifier); a real name is trimmed and rendered with a spaced em dash (`Bottle 2 — Sarah`), preserving Unicode/accented characters as-is.
- **Both guess-entry pages use the shared helper exclusively** — no raw string concatenation is duplicated per component. Full blind's progress-bar label and `WineGuessForm` bottle-code both use it; course-reveal's progress-bar label, `WineGuessForm` bottle-code, and the locked-guess screen's eyebrow label all use it too. Course-reveal's *revealed-waiting* transition screen (which is about to show the actual wine reveal) and the reveal card itself are deliberately left alone, since they already carry their own intended wine-identity display, not a bottle-order label.
- **Course-by-course participants only ever see the label for their own currently-active, already-authorised bottle** — `get_active_bottle_state` already scopes `activeBottle` to the one bottle the participant is currently permitted to see, so this feature adds no new way to see a not-yet-released bottle's contributor or anything else about it.

## Data model notes: grape/blend, price band, and controlled country/region

- **Grape/blend** replaces the old free-text "grape/style" field everywhere in the UI. The underlying database column is still named `grape_style` (kept for migration safety — renaming a live column is riskier than mapping it internally); it now holds the canonical single-variety name or the flattened, alphabetised blend text (see "Structured grape/blend" above). A new nullable `grape_blend_mode` column (`'single' | 'blend' | null`) was added; `null` means the row predates this feature, and scoring/display fall back safely rather than guessing. See `lib/wineReferenceData.ts` for the curated grape list and alias table.
- **Price band** has been removed from all new bottle registration, guess entry, scoring, and results display. The `price_band`/`price_band_guess` database columns and any existing stored values are left in place untouched (the columns are simply no longer required or written) — nothing destructive happens to old data, it's just unused.
- **Country and region** are now controlled dropdowns (`lib/wineReferenceData.ts`), region filtered by the selected country. Both are stored as their canonical **display name** (e.g. `"France"`, `"Bordeaux"`) in the existing `country`/`region` text columns — deliberately *not* an ISO code — so a bottle registered before this feature (free-text country/region) and one registered after it remain directly comparable via the same text-normalisation matcher used everywhere else, with no separate code-to-name lookup needed for scoring or display. See the completion notes in `SUPABASE_SETUP.md` for the full rationale.
- **Wine style** (`wines.wine_style`) is a required, contributor-set classification (Bubbles/White/Red/Sweet/Other) — not scored or guessed, just a category shown to the contributor for their own bottles, to the host alongside the bottle's anonymous number only, and in revealed results. Existing bottles from before this feature backfill to `'other'`.
- **Bottle number vs. tasting order**: `bottle_number` (e.g. "Bottle 3") is the bottle's permanent anonymous identity — assigned once at registration from a monotonic per-session counter, and never reused or changed, even after a delete. `tasting_order` is a *separate*, mutable 1..N sequence the host can freely rearrange during registration (via Move up/down controls on the host page) to control the order bottles are actually poured and guessed in; it's contiguous and unique per session, defaults to registration order, is re-packed automatically when a bottle is deleted, and is frozen the moment the host starts tasting. A bottle keeps the same permanent number regardless of where it sits in the tasting order — e.g. "Bottle 3" might be tasted 1st.

## Region and Appellation

The single `Region / appellation` field has been split into two:

- **Region** — the same field as before, broad geographical region (e.g. Burgundy, Piedmont, Napa Valley), unchanged in meaning and still stored in the existing `region` column.
- **Appellation** — a new, optional, more specific recognised appellation within that region (e.g. Chablis within Burgundy), stored in a new nullable `appellation` column on `wines` and `cellar_bottles`.

**Curated, not comprehensive.** Appellation options come from a small, hand-picked v1 list in `lib/appellations.ts` — a deliberate design choice, not an oversight. Only ~30 country/region pairs are supported (see that file for the full list: most of France, Piedmont/Tuscany/Veneto/Sicily/Campania in Italy, Rioja/Ribera del Duero/Priorat/Rías Baixas/Jerez in Spain, Douro/Dão/Alentejo in Portugal, three regions each in Germany and Austria, California/Oregon/Washington in the US, four states in Australia, and three regions in New Zealand). Every other country/region combination simply has no Appellation field at all — this is expected, not a bug.

**Conditional visibility.** The field is hidden by default and only appears once both a supported country and region are selected (`hasAppellations(country, region)`); it always includes a blank "Select an appellation" option and is never required. Changing region (or country, which can also change region) always clears any selected appellation — a value chosen for Burgundy is never silently carried over to a different region.

**Adding a new supported region/appellation pair:** add the entry to `APPELLATIONS_BY_COUNTRY_REGION` in `lib/appellations.ts` (the region string must exactly match an existing `REGIONS_BY_COUNTRY` entry in `lib/wineReferenceData.ts` — add one there first if needed), then mirror the identical entry in the `is_valid_appellation` function's jsonb literal in `supabase/schema.sql`, since the server independently re-validates every appellation against its own copy of this list rather than trusting the client's dropdown (`register_bottle`, `update_bottle`, `add_cellar_bottle`, `update_cellar_bottle` all call it). Two region keys were deliberately adjusted from the most casual spelling to match this app's own existing region vocabulary — see the comment at the top of `lib/appellations.ts` for exactly which, and why.

**Not used for grape selection or blend behaviour.** Appellation never influences grape/blend auto-selection or filtering. Scoring involvement now depends entirely on which session it's in — see README "Scoring model": under `legacy_v1` (every session created before that feature), a participant's Appellation guess (see "Blind-guess Appellation" below) is never scored, purely an unscored guess-vs-actual comparison in the reveal/report. Under `core_v3_appellation_conditional` (current), the actual wine's Appellation set here is exactly what a guess is compared against for up to 20 scored points — conditionally excluded from the denominator entirely when this field is left blank on the actual wine.

**Privacy.** Appellation follows the exact same visibility rules as region/country: hidden in every pre-reveal view, masked the same way in `guest_visible_wines`, and only ever shown once a session is revealed (or, for Seen tastings, once the tasting starts) — see "Tasting modes" above. It also never appears in Personal Cellar's `/cellar` views to anyone but the owner, same as every other cellar field.

**Historic records are not backfilled.** Every bottle registered before this feature simply has a `null` appellation forever — there is no attempt to guess or infer one, and forms edit such a bottle exactly as before, just with the Appellation field starting blank (and hidden, if the bottle's country/region pair happens to have no curated list).

**Display.** Where wine identity is already shown as separate structured fields (the course-reveal per-bottle reveal screen), Appellation appears as its own row, hidden entirely when null — never a fabricated "Not specified". Everywhere else (tasting report rankings, the wine ledger, the cellar list), a concise `Appellation, Region, Country` string is used instead (`compactWineLocationLabel` in `lib/appellations.ts`), omitting missing values and never duplicating when region and appellation share the same display text (e.g. an unsplit "Champagne").

### Blind-guess Appellation

The same optional Appellation concept above is also offered on the participant's **blind guess**, in both Full-blind and course-reveal (the two modes share one guess-entry component, `components/WineGuessForm.tsx`) — Seen tasting has no blind guesses, so it is untouched.

- **Same curated map, zero duplication.** The guess field reuses `getAppellations`/`hasAppellations` from `lib/appellations.ts` and the server reuses `is_valid_appellation()` verbatim — there is no second list. It follows the identical conditional-visibility and reset rules as the actual-wine field above: hidden until a supported country guess and region guess are both chosen, and clearing/changing the region guess always clears the appellation guess (with an accessible "Appellation cleared because the region changed." announcement), while changing the grape/vintage/producer/wine/rating/note fields never touches it.
- **Scoring depends on the session's scoring version — see README "Scoring model".** The guess is always stored the same way (`wine_guesses.appellation_guess`, additive/nullable), but what happens to it at reveal time now differs: under `legacy_v1` sessions (every session created before the scoring-model replacement), it remains a plain, permanent passthrough — `ScoredGuess.appellationGuess`, never a `FieldScore`, never affecting `corePoints`/`bonusPoints`/`totalPoints`, no correctness computed. Under `core_v3_appellation_conditional` sessions (current), it is instead a genuine scored core category worth up to 20 points, conditionally excluded from the denominator when the actual wine has no recorded Appellation — see "Scoring model" for the exact rules. This is not a retroactive change: which behaviour applies is fixed forever by whichever version the session was created with.
- **Reveal display.** Under `legacy_v1`, an unscored `Appellation` comparison block appears in the per-guest breakdown (`WineResultCard`'s exported `AppellationComparison`, reused by the course-reveal per-bottle page) only if either the guess or the actual appellation is non-empty — a plain two-value "Guess: … / Wine: …" comparison, with no correct/incorrect label, no color coding, and no points. Under `core_v3_appellation_conditional`, Appellation instead appears as an ordinary scored row in the Core categories table (or "Not applicable" when the actual wine has none) — see "Scoring model". (The task spec that introduced this feature used example copy "Your call:" — this app has no existing concept of "which guess is the current viewer's own" anywhere in its post-reveal report, which is shared identically among all participants, so "Guess:" was used instead to avoid mislabeling someone else's guess as the viewer's.)
- **Privacy.** Follows the exact same pre-reveal secrecy as every other guess field — never exposed to other participants or the host before reveal, never validated against a second, looser rule than the actual-wine field, no RLS changes.
- **Historic guesses are not backfilled** — every guess submitted before this feature simply has a `null` appellation guess forever.

## Multi-device architecture

Sessions live in Supabase Postgres. See [SUPABASE_SETUP.md](SUPABASE_SETUP.md) for the full setup, the bottle-numbering concurrency approach, and the security model (host/guest tokens, Row Level Security, and why there's no service-role key anywhere in this app). This is the model every tasting workflow uses regardless of whether anyone is signed in — see "Accounts" below for the separate, optional identity layer that sits alongside it. In short:

- The browser only keeps two things locally: the **host token** for tastings this device hosts, and the **guest token** for tastings this device has joined (`lib/deviceStorage.ts`) — the host's own participant token is stored in the same slot a regular guest's would be, so registration/tasting pages need no host-specific branching. Everything else — session, bottles, guesses — is fetched live from Supabase.
- Host-only mutations (create session, start tasting, reveal) go through Next.js Route Handlers (`app/api/host/*`), which validate the host token server-side.
- Participant actions (join, register/edit/delete a bottle, autosave a guess, final submit) call `SECURITY DEFINER` Postgres functions directly from the browser using the public anon key; the functions validate the guest token and bottle ownership inside Postgres.
- Supabase Realtime pushes live status/bottle-count/submission-count updates to the host and registration pages, and reveal transitions to the tasting/results pages.

## Tasting archive

A private, read-only `/archive` page lets a host see every tasting they've hosted ("Hosted by you") and a participant see every tasting they've joined ("Joined by you") from **this browser**, once each one is revealed. It is deliberately browser-linked, not account-linked — there's no sign-in in this app (see "Multi-device architecture" above), so "who can see this in their archive" is defined the same way every other permission in this app already is: possession of a valid host or guest token.

- **How it works**: `lib/deviceStorage.ts` already stores a host/guest token per session it creates/joins; it now also appends a tiny `{ publicId, role, lastSeenAt }` reference to a local index (`blindCellar.archiveRefs`) whenever a token is stored — no raw token is ever duplicated into this index, it only ever points back at the existing per-session token slot. The archive page reads that index, resolves each reference's token, and POSTs `{ publicId, role, token }` triples to `POST /api/archive/lookup`.
- **How access is validated**: that route calls the exact same `SECURITY DEFINER` RPCs every other host/guest action already uses — `get_host_session` for a host reference, `get_guest_session_state` for a participant one — so a stale or forged token is rejected by Postgres itself, the same as it would be anywhere else in the app. A reference only becomes a visible archive entry once its RPC call succeeds *and* the session's `status` is `revealed`; a valid token for a not-yet-revealed session is kept locally but simply produces no entry yet. If a session is reachable through both a host and a participant reference (the host is always also a guest in their own session), it's shown once, under "Hosted by you."
- **What it reuses, not duplicates**: Wine of the Night and blind-identification accuracy come from the same `buildTastingReport`/`buildSeenTastingReport` pipeline the results page uses (now shared via `lib/supabase/reportData.ts`), and "View report" opens the *existing* `/results/[publicId]` page — the archive adds a "FROM THE ARCHIVE" marker and a "Back to archive" link there, nothing else. It does not change who can open a results link; that has always been "anyone who knows the link, once revealed" (see "Security model, in plain English" in `SUPABASE_SETUP.md`) — the archive only changes *discovery* (which sessions a given browser is shown), never report access itself.
- **Browser/device limitation**: this is genuinely a "this browser only" feature. A tasting hosted or joined on one phone won't appear in the archive on another device, or after clearing site data. This remains true even for a signed-in user in this phase — see "Accounts" below: signing in does not yet reattach this browser's archive to anything. The archive page says so directly: *"Your archive is available on this browser. Account-based access and cross-device history may be added later."*
- **Removing a local archive reference**: since there's no server-side record of "this browser's archive" beyond the tokens it already holds, clearing that browser's site data/localStorage for this app (or just for the `blindCellar.archiveRefs` / `blindCellar.hostToken.*` / `blindCellar.guestToken.*` keys) removes it from that browser's archive — there's nothing to delete on the server, since the archive was never a server-side list in the first place.
- **Future path (not implemented)**: linking this archive concept to the account layer described below, so it can follow a person across devices instead of staying pinned to one browser, is tracked as a Phase 2 follow-up — see "Accounts" and "Recommended next features" below.

## Accounts

An **optional**, passwordless account layer sits alongside the host/guest token model above — see SUPABASE_SETUP.md "Setting up Supabase Auth (accounts)" for the Dashboard configuration. Nothing about hosting or joining a tasting requires signing in; this is purely a foundation for a future cross-device tasting record.

- **Sign-in method**: email one-time code (OTP) via `supabase.auth.signInWithOtp()` / `verifyOtp()` — no passwords, no social login, no magic link (see SUPABASE_SETUP.md for why OTP was chosen and how magic links could be enabled instead). `/account/sign-in` is a single page with two steps (enter email, then enter the 6-digit code); `/account` shows the signed-in email, an optional account-level display name, a link to the archive, and sign out.
- **What signing in does NOT do by itself**: it does not retroactively migrate, attach, or reveal any of this browser's *existing* host/guest-token tastings to the account — those stay exactly where they were, browser-linked. What signing in *does* enable going forward, and for explicit historic claims, is described in "Account-linked tasting records" below.
- **Identity model**: a `public.profiles` row (`id` referencing `auth.users(id)`, `email`, optional `display_name`) is created automatically the moment someone verifies their first sign-in code — see `handle_new_user()` in `supabase/schema.sql`. This is deliberately the *only* thing an account layer touches; no `wines`/`tasting_sessions`/`guests`/`wine_guesses` row is ever linked to `auth.users` in this phase, and no tasting RPC gained an `auth.uid()` check.
- **A signed-in browser still uses the exact same anonymous RPCs and views** (`join_tasting_session`, `register_bottle`, `upsert_wine_guess`, `guest_visible_wines`, …) that an anonymous one does — see "Security model, in plain English" in `SUPABASE_SETUP.md` for the grant change this required (`authenticated` needed the same narrow access `anon` already had, nothing more) so that signing in never breaks or changes tasting behaviour.
- **Session storage**: the browser Supabase client (`lib/supabase/client.ts`) now uses `@supabase/ssr`'s cookie-based session storage instead of plain `localStorage`, and a minimal `middleware.ts` refreshes that session cookie on navigation. This only affects the Auth session itself — host tokens, guest tokens, and the archive's local reference index are completely untouched and still stored exactly as before.
- **Account display name vs. tasting display name**: the optional name on `/account` (`profiles.display_name`) is entirely separate from the name a guest types when joining a specific tasting (`guests.display_name`) — saving one never touches the other, and an existing session's guest name is never silently replaced.
- **Sign-out** clears only the Supabase Auth session (cookies) — it never touches `blindCellar.hostToken.*`, `blindCellar.guestToken.*`, or `blindCellar.archiveRefs`, so anonymous host/guest access and the local archive keep working immediately afterward.

## Account-linked tasting records

Builds on "Accounts" and "Tasting archive" above: a signed-in user can keep a durable, cross-device record of the tastings they host or join, and can explicitly add eligible completed tastings from this browser's existing browser-linked archive to that record. **Account linkage never replaces host/guest token authorization** — it is a second, independent "does a durable association exist" record layered on top, purely for *discovery* (which sessions show up in your archive on a new device), not a new permission. Nothing about the existing report-access model, RLS on tasting tables, or token validation changes.

- **Data model**: `public.account_tasting_records` (`user_id`, `session_id`, `role` — `'host'` or `'participant'` — `participant_id`, `claim_source` — `'automatic'` or `'browser_claim'` — `claimed_at`, `created_at`). Enforced by two partial unique indexes (one host link per user/session; one participant link per user/session/participant) so retries can never duplicate a row. It never stores a host token, guest token, Auth token, OTP code, or any tasting content (wines, guesses, ratings, results) — see `supabase/schema.sql`.
- **RLS**: `select using (auth.uid() = user_id)` and nothing else — no insert, update, or delete policy at all, for any role, including the row's own owner. The only way a row is ever created is `claim_account_tasting_record(p_public_id, p_role, p_token, p_claim_source)`, a `SECURITY DEFINER` RPC granted to `authenticated` only. Like every other RPC in this file, it's directly reachable via the anon key regardless of who calls it, so it independently re-validates the actual host/guest token against the exact session every time — a signed-in user who merely *knows* a session id, with no valid token for it, always fails. See SUPABASE_SETUP.md for the exact policy/RPC text.
- **Automatic linking**: when a signed-in user creates a tasting (`app/api/host/create-session`) or joins one (`app/join/[publicId]/page.tsx`), the exact same anonymous host-token/guest-token flow runs completely unchanged, and a `claim_source = 'automatic'` record is added afterward as a best-effort side effect — using a *separate*, cookie-aware Supabase client (`lib/supabase/routeHandlerClient.ts`) only to learn "is anyone signed in," never in place of the anon-key client the tasting RPCs themselves already use. If that link fails (or nobody is signed in), the tasting/join itself is completely unaffected; a host only ever sees a subtle, dismissible "…could not yet be added to your account record" notice, and a joining guest sees nothing at all (join errors are never surfaced for this).
- **Historic claims**: on the original browser, a signed-in user can click "Add to my tasting record" on an eligible completed (`revealed`) session in the browser archive, or on its report page — both call the same `claim_source = 'browser_claim'` path, gated additionally on the session actually being `revealed` (checked inside the RPC itself, not just client-side). "Eligible" always means: signed in, this exact browser still holds a valid host or guest token for this exact session, and the account hasn't already claimed it in that role.
- **Why `claim_source` isn't itself a security boundary**: both automatic and historic linking go through the identical token-validation path; the label is metadata for the revealed-status check and future auditing, not a privilege distinction. What actually governs whether a link is *visible* in "Your record" is the linked session's current status, re-checked at read time on every load — so an automatically-linked, still-`collecting` session simply doesn't show up yet, with no separate cleanup step needed once it's later revealed.
- **Same-browser requirement for claims**: identical to the browser archive's own limitation — a claim can only ever be made from the browser that still holds the actual host/guest token, since that token is what the claim proves. There is no way to claim a session from a device that never held it.
- **Cross-device behavior**: `/archive` shows two tabs for a signed-in user — **Your record** (everything linked to the account: automatic + successfully claimed, read directly from `account_tasting_records` under RLS, filtered to currently-revealed sessions, deduped host-over-participant, sorted the same way the browser archive is) and **This browser** (the existing browser-linked list, now also showing a quiet "Add to my tasting record" action on eligible unlinked entries, or "Already in your record" / "Available on this browser only" otherwise). Signing in on a second device shows only "Your record" — that device's own "This browser" tab starts empty, exactly as it should.
- **Report access is unchanged**: opening a report from either tab reuses the exact same `/results/[publicId]` route and report components (`TastingReportView`/`SeenTastingReportView`) — see "Multi-device architecture" above for why that route has never required a token to view a *revealed* report. "Your record" only adds a `FROM YOUR RECORD` / "Back to your record" marker (`?from=account`), the same way the browser archive already adds `FROM THE ARCHIVE` / "Back to archive" (`?from=archive`) — a display-only marker, never an authorization check.
- **What this phase intentionally does not do**: no "claim everything at once" bulk action (every claim is validated individually); no account merging between two different signed-in identities; no way to un-link or delete a record; no migration of pre-existing browser sessions just because someone signs in later; no change to what an anonymous browser, with no account at all, can already do.

## Palate Profile

A private, account-linked `/profile` page ("Your palate") that helps a signed-in user understand their own tasting activity, blind-tasting performance, wine preferences, and full tasted-wines ledger — a personal cellar ledger, never a social profile, leaderboard, or gamified score screen. Builds entirely on "Account-linked tasting records" above: every figure comes from the signed-in user's own `account_tasting_records`, re-read and re-aggregated server-side on every load, never a separately stored or cached "profile stats" row.

- **Tasting scope toggle**: a persistent, per-user preference — `Blind tastings only` (default) or `Include Seen tastings` — that governs every tasting/rating summary on the page (At a glance, Your wine record, the Tasted Wines Ledger, Recent evenings). **Blind palate never changes with this toggle** — it is always built from Full blind and Course-by-course sessions only, because a Seen-tasting participant already knows each bottle's identity before rating it, so a Seen rating is valid preference data but never valid blind-identification data. This is enforced structurally, not just by convention: `computeBlindPalate` is always fed a separately-filtered observation set (`extractBlindObservations`), which drops every Seen-tasting observation unconditionally regardless of what the caller passes anywhere else.
- **Data source and authorization**: every profile query starts from `account_tasting_records` under its existing RLS (`auth.uid() = user_id`) — never browser-only unclaimed archive records, never a session merely present in local storage, never an unrevealed/registration/collecting session, never another user's records. A signed-in host who also participated in their own tasting is counted once, not twice: see "Duplicate handling" below.
- **Duplicate handling**: when a session has both a `role = 'host'` and a `role = 'participant'` link for the same user (the ordinary case for a host who also joined their own session, plus the rarer case of joining the same session twice under different names), `resolveSessionLinks` (`lib/profile.ts`) collapses them into one `SessionLink` — the session counts once toward attendance, appears once in Recent evenings, and its ratings/guesses always come from the linked *participant* guest row, never a second one. A host-only link (no separate participant record) still counts toward hosted activity, and its own ratings/guesses are recovered from the session's `host_guest_id` — see the new grant below — since the host's own guess entry uses that same guest row under the hood (see "Multi-device architecture").
- **New, narrow grant — `tasting_sessions.host_guest_id`**: added specifically so the profile can resolve a host's own guest row without trusting anything client-provided. This is *not* new privacy exposure: every guest id (host or otherwise) is already fully readable by anyone via the existing open `guests` grant (`id, session_id, display_name, ...`) — this only reveals *which already-public guest id* belongs to the host, for a session the caller already independently holds an `account_tasting_records` link to. This is a deliberate small widening of the asymmetry noted in "Account-linked tasting records" above (the archive's host-role entries omit accuracy for the same reason this grant didn't exist yet); the Palate Profile needed the real figure enough to justify the grant, the archive did not.
- **Scoring is never recomputed**: every session's report is built via the exact same `loadTastingReportData` → `buildTastingReport` / `buildSeenTastingReport` → `scoreWineGuess` pipeline the final-report page and archive already use (`lib/supabase/profileData.ts`), and each session's own stored `scoring_version` governs which model that call uses (see README "Scoring model"). The profile only reads back each user's own `ScoredGuess` out of that existing report — "Blind accuracy" and per-category accuracy come directly from `ScoredGuess.totalPoints`/`totalPossiblePoints`/`fieldScores`, safely blending `legacy_v1` and `core_v3_appellation_conditional` guesses because each guess already carries its own correct possible-points denominator. There is no bonus-category/hit-rate figure under the current model, so none is shown; Appellation accuracy is aggregated separately and only counts guesses where the actual wine had one recorded.
- **Best-effort unique-wine matching**: two observations are the same wine only if producer, wine/cuvée, vintage, region, *and* country all match after normalization (lowercase, trim, collapse whitespace, accent/punctuation-insensitive — reusing the existing `normalizeText`, the same matcher blind scoring already uses). A partial match (e.g. same producer and vintage but different region) is always treated as two separate wines, never merged — see `normalizeWineIdentityKey` in `lib/profile.ts`. Original recorded values are always preserved for display; the normalized key is used for aggregation only.
- **Sample thresholds for preference conclusions** (never shown as a false claim from too little data): "Highest rated" categories require at least 3 rated observations; "Familiar ground" (producers/wines) requires at least 2 observations; "Most revisited" requires at least 2 distinct tasting *sessions* (not just 2 observations, which could otherwise double-count a single evening); Blind palate's "strengths/developing areas" readout requires at least 10 total submitted blind calls *and* at least 5 eligible calls in a specific category before naming it a strength or developing area.
- **The Tasted Wines Ledger is paginated, filtered, and sorted entirely server-side** (`GET /api/profile/ledger`) — the browser never downloads more than one page. Filter option lists (countries, styles, formats, years present) are derived only from the same already-scoped rows, so a filter dropdown can never reveal a value that exists only in someone else's data. For this app's realistic per-user data volume, "server-side" here means the Route Handler loads this one user's own account-linked sessions (never global data) and paginates in memory before responding — see "Recommended next features" for the true database-level pagination this would need at a much larger personal scale.
- **Report links from the profile** (`?from=account`) reuse the exact same `/results/[publicId]` route, `FROM YOUR RECORD` banner, and report components as "Your record" in the archive — no separate report engine, no separate authorization rule.
- **Navigation**: "Your palate" appears in the signed-in home header, the account page, the archive page, and a *revealed* report's nav row only — never on bottle registration, live host controls, active blind-guess entry, or any pre-reveal screen.
- **What this phase intentionally does not implement**: public profiles or profile URLs, social follows/leaderboards/comparison between users, badges/streaks/ranks/achievements, AI-generated notes or recommendations, PDF export, wine-marketplace or external wine-API integration, un-linking a specific tasting from the profile, and true cursor-based database pagination for the ledger (see above).

## Personal Cellar

A private, account-owned inventory of physical bottles at `/cellar` ("My cellar"), with a safe way to pull one of them into a tasting's existing bottle-registration flow. Wholly separate from the anonymous host/guest token model: a cellar bottle belongs to exactly one `auth.users` row, is never readable by anyone else, and only ever becomes visible inside a tasting indirectly — as the already-anonymous `wines` row it was used to create.

- **Physical-bottle model**: one `cellar_bottles` row represents exactly one physical bottle — never a quantity integer, even after "Quantity" below. Three bottles of the same wine are three separate rows. There is no bulk/CSV import, barcode scanning, OCR, or photo capture in v1; adding a bottle is a short manual form reusing the exact same wine-identity fields and validation as tasting bottle registration (`WineIdentityInput`, shared by both forms — see `components/registration/WineIdentityFields.tsx`).
- **Quantity (add-only convenience, not a stock count)**: the "Add a bottle" form has a `Quantity` field (default 1, integer, 1–100) so registering several identical physical bottles — e.g. a case of six — doesn't require repeating the form six times. Entering a quantity greater than 1 creates that many separate `cellar_bottles` rows in a single atomic server-side operation (`add_cellar_bottle`'s `p_quantity` parameter, validated and clamped server-side regardless of what the client sends) — it never stores "6" as a count on one row. Each created row is independently reservable, returnable, and consumable from the moment it's created, exactly as if it had been added one at a time; reserving or consuming one of a batch never affects the others. Quantity only exists on the add form — it's never shown or editable on an existing bottle (editing a bottle always edits that one physical unit, and `update_cellar_bottle` has no quantity parameter at all), and it is never itself persisted anywhere as a field on `cellar_bottles`.
- **Status lifecycle**: `available` → `reserved` (selected for a tasting; not yet used or returned) → `consumed` (used; permanent, never reverts). `reserved` can also return to `available` if the bottle is removed from its tasting before that tasting begins. All three shapes are enforced by a single database check constraint (`cellar_bottles_status_shape`) — every write path produces exactly one of the three, never a partial/inconsistent combination.
- **No direct client mutation at all**: unlike `profiles`, `cellar_bottles` has *no* insert/update/delete RLS policy for any role — not even "owner may update their own row." Every mutation (add, edit, reserve, return, consume) goes through a `SECURITY DEFINER` RPC that independently re-validates `auth.uid()` and ownership, since (like every RPC in this file) it's reachable directly via the anon key regardless of who calls it. Reads are plain RLS (`auth.uid() = owner_user_id`) — the same direct-browser-query pattern "Your record" already uses.
- **Reservation happens only at successful registration, not at selection** — clicking a cellar bottle in the selector just sets local UI state; the actual `available → reserved` transition and the new `wines` row are created together, atomically, inside `register_bottle_from_cellar` only once the participant confirms "Use this bottle." Two concurrent attempts to use the same cellar bottle can't both succeed: the RPC takes a row lock (`select ... for update`) on the cellar bottle before checking its status, so a second concurrent call blocks until the first commits, then re-reads the now-`reserved` row and fails safely with the same generic message a plain "no longer available" failure would show (`cellar_bottle_unavailable` never distinguishes "not yours" from "already used" from "doesn't exist").
- **Returning a bottle reuses the existing pre-tasting bottle-removal path, not a new one**: the existing `delete_bottle` RPC (used by the registration page's "Delete" button, unchanged) was made cellar-aware — if the bottle being deleted has a `cellar_bottle_id`, it atomically restores that cellar bottle to `available` in the same transaction. This means the existing UI needed **zero changes** to correctly "return to cellar" when a contributor removes their own cellar-linked bottle before the tasting starts. A second RPC, `return_cellar_bottle_to_available`, covers the same action from `/cellar` itself (the Reserved tab's "Return to cellar" button) — authorized by `auth.uid()` + cellar ownership alone, with no guest token involved, since the signed-in owner may not be on the browser that holds one. Both paths are blocked once the session leaves `registration` status (i.e., once the tasting itself has started).
- **Consumption is a deliberate, explicit action** (`Mark bottle as consumed`, shown only to the cellar owner on their own Reserved entries), never automatic — a reservation existing is not proof a bottle was actually opened. It's only permitted once the linked session has reached `revealed` (this app's "completed" state), so a bottle can never be marked consumed while its tasting hasn't started or is still in progress.
- **Cellar provenance is invisible everywhere except the owner's own `/cellar` page**: `wines.cellar_bottle_id` has no `anon`/`authenticated` column grant, so the only way it's ever read is from inside a `SECURITY DEFINER` function that explicitly selects it. The host, other participants, blind-guess screens, and final reports see a cellar-sourced bottle exactly like a manually-registered one — same anonymous number, same reveal timing, same everything. Storage location and personal notes never leave `cellar_bottles` at all; they're never copied onto the `wines` row.
- **Registration integration**: signed-in participants with at least one `available` cellar bottle see a segmented "Add a new bottle" / "Add from my cellar" choice at the top of the existing add-bottle page, defaulting to the existing manual flow. Choosing a cellar bottle shows a read-only confirmation (wine identity, format, "this bottle will be reserved…") with "Use this bottle" / "Choose another bottle" / "Add a new bottle instead" — wine-identity fields are never re-editable after a cellar bottle is selected, to preserve provenance between the physical bottle and the tasting bottle it becomes. Signed-out visitors and signed-in users with an empty cellar see only the unchanged manual flow, with a small non-blocking note — never a blocked or confusing registration.
- **Deleting a bottle is Available-only, permanently**: only a bottle with `status = 'available'` may ever be edited or deleted. `/cellar` shows Edit (pencil) and Delete (trash) icon buttons beside the bottle name only for Available rows — Reserved and Consumed rows show neither icon, and no empty space is reserved where they would have been. Deleting asks for confirmation ("Delete this bottle?" / "This will permanently remove it from your cellar.") and, once confirmed, permanently removes that one `cellar_bottles` row via `delete_cellar_bottle`, which re-validates ownership and `status = 'available'` itself with a single conditional `delete ... where id = ... and owner_user_id = auth.uid() and status = 'available'` — never trusting the client's view of the bottle's status. A Reserved bottle must first go through the existing "Return to cellar" flow (see above) before it becomes eligible for deletion; a Consumed bottle can never be deleted, returned, or otherwise removed — it is a permanent cellar-history record in v1. There is no trash, archive, or restore feature for a deleted bottle.
- **What this phase intentionally does not implement**: bottle price/purchase-price tracking, market valuations, external wine-API enrichment, AI recommendations, drink-window analytics, CSV/bulk import, barcode/OCR scanning, bottle photos, shared/household cellars, deleting a Reserved or Consumed bottle, editing a bottle's identity once it's reserved or consumed, and a full tasting-cancellation feature (this app has no session-cancellation status today — "return to cellar" is scoped to the existing pre-tasting `registration` window instead; see "Assumptions and design choices").

### Grouped display

Both `/cellar` and the Add-from-cellar picker (`components/registration/CellarBottleSelector.tsx`, used from `/register/[publicId]/new`) visually group matching physical bottles into one row with a bottle count. This is a purely visual, read-time selector — nothing about the underlying storage model changes:

- **Storage is unchanged**: one `cellar_bottles` row is still exactly one physical bottle. Grouping never merges, migrates, or deletes rows, and adding a bottle still creates one row per bottle exactly as before (see "Quantity" above).
- **Shared grouping helper**: `lib/cellarGrouping.ts`'s `groupCellarBottles(rows)` is the single grouping selector both screens call, over the exact same already-fetched, already-owner/status-scoped `CellarBottleRow[]` array each screen already loaded (there is no separate grouped-listing RPC or view — grouping happens client-side, purely as a display selector, since both callers' underlying queries were already correctly scoped). Two rows group only when every one of these fields matches, after trimming/collapsing whitespace and comparing case-insensitively (no accent-stripping or other fuzzy matching — see `lib/cellarGrouping.ts`'s `buildCellarGroupKey`): status, producer, wine/cuvée, vintage, country, region, appellation, grape/blend mode, grape/blend text, structured blend components (tolerant of pick order, not of a materially different grape set), wine style, bottle format (+ its free-text detail when "other"), and storage location. Different owners can never group at all — `CellarBottleRow` carries no `owner_user_id` client-side (RLS already scopes every fetch to `auth.uid()`), so there is no cross-owner array for the grouping function to ever receive.
- **Personal notes are excluded from the group key on purpose**: two otherwise-identical bottles with different notes still group together, and each underlying row's note is left completely untouched — grouping never reads, merges, overwrites, or discards a note; it's simply never part of what the grouped UI displays.
- **Group DTO is minimal by construction**: a `CellarBottleGroup` carries a `groupKey`, `status`, `bottleCount`, its `representative` row (for display fields), and its underlying `bottles` (used only internally, by the expansion UI and by the atomic add action's anchor selection — never rendered as raw IDs, logged, or put in a URL).
- **My Cellar row layout**: `Producer — Wine/cuvée` (with Edit/Delete only for a one-bottle Available group), then `Vintage/NV · Appellation, Region · Country`, then `Format · Storage location`, then a plain-text `Available · 6 bottles` / `Reserved for tasting · 2 bottles` / `Consumed · 1 bottle` line — never `Qty`, `x6`, or a colour-only badge. Reserved/Consumed groups show the existing per-bottle session-title/date context line (and, for Consumed, the "View tasting report" link) only when the group has exactly one bottle — with more than one, which session to name would be ambiguous, so only the generic status·count line is shown.
- **Multi-bottle Available groups expand rather than act on everything at once**: a group of one bottle keeps the exact, unchanged single-bottle `CellarEntryRow` with its existing direct Edit/Delete. A group of more than one bottle shows a "Show N bottles" toggle that reveals every underlying physical bottle as its own, fully independent `CellarEntryRow` — same component, same Edit/Delete/Return/Consume handlers, just one per real row. This was chosen over a "delete one bottle" confirmation dialog naming a server-resolved bottle: reusing the existing single-bottle component for every expanded row means zero new server-side "which one bottle" resolution logic was needed for Edit/Delete/Return/Consume, and a user always sees and acts on one concrete, already-visible physical bottle — never a group action silently touching more than one row.
- **Add-from-cellar picker row layout**: same identity/origin/format lines, ending in `6 bottles available` / `1 bottle available` (correct singular/plural grammar, never a duplicate row per physical bottle). Only the current owner's `available` bottles are ever shown — no notes, no raw cellar/tasting IDs, no other user's data.
- **Status totals stay physical-bottle totals**: `/cellar`'s per-tab count (`Available (8)`) is always `rows.length` from the tab's unfiltered fetch, never the number of *visible groups* — filtering below only narrows which grouped rows are shown, it never changes this number.

### Cascading filters

A shared filter bar (`components/cellar/CellarFilterBar.tsx`, backed by pure logic in `lib/cellarFilters.ts`) is reused, unchanged, by both `/cellar` and the picker — the only difference between the two is which already-scoped `CellarBottleGroup[]` array is passed in (one status tab for My Cellar; Available-only for the picker).

- **Four filters, in cascade order**: **Type** → **Country** → **Region** → **Appellation**, each defaulting to "All ...". Type reuses the cellar's existing five canonical `wine_style` values (Bubbles/White/Red/Sweet/Other) — the schema's real classification — rather than inventing a separate Rosé/Sparkling/Orange/Fortified vocabulary that doesn't exist in any stored field.
- **AND semantics, single-select per dimension, apply immediately** — no "Apply filters" button, no multi-select chips.
- **Cascading options**: each dropdown's option list is scoped only by its parents (Country by Type; Region by Type+Country; Region by Type+Country+Region) and is built fresh from the same `groups` array — never a second, divergent data source. A blank/null region or appellation is never offered as a selectable value.
- **Ambiguous labels, unambiguous values**: if the same region name is shared by more than one country among the currently eligible groups (or the same appellation by more than one region/country), that option's *label* gains a disambiguating suffix (`Region — Country A / Country B`, `Appellation — Region, Country`) — but the underlying filter *value* stays the plain string, since filtering only ever needs exact-field equality, not a synthetic composite key.
- **Dependent resets, never a stale selection**: changing Type may reset Country/Region/Appellation; changing Country may reset Region/Appellation; changing Region may reset Appellation; changing Appellation never touches its parents. A dependent value is kept only if it's still among the valid options computed from the new parent values — otherwise it resets to "All ..." (`lib/cellarFilters.ts`'s `updateCellarFilter`). The same reconciliation also runs whenever the underlying data changes for any other reason (tab switch, refetch, a mutation refresh) — a no-op when nothing went stale, so a filter choice survives a same-view refresh, but is never left silently selected against zero options.
- **Filtered-empty vs. no-inventory-empty are distinct**: if the current tab/view genuinely has zero bottles, the existing no-inventory empty state is shown unchanged. If bottles exist but none match the active filters, a separate `No bottles match these filters.` state appears with a `Clear filters` action that resets all four filters — it never touches any inventory data.

### Atomic multi-bottle add

Selecting a grouped Add-from-cellar entry with more than one available bottle lets a participant add several in one action, instead of repeating "Add another bottle" N times.

- **One bottle, no visible quantity field**: a one-bottle group still shows a streamlined `Add to tasting` action and goes through the exact same, unmodified `register_bottle_from_cellar` RPC this feature already had — zero new risk for the by-far-most-common case.
- **More than one bottle**: a `Bottles to add` integer field (default 1, min 1, max the group's current available count) feeds a new RPC, `register_bottles_from_cellar_group(p_guest_token, p_cellar_bottle_id, p_quantity)`, modeled directly on `register_bottle_from_cellar`'s own locking/authorization pattern. `p_cellar_bottle_id` here is only an *anchor* — one bottle the client already legitimately has from its own RLS-scoped fetch — the RPC always recomputes the actual matching group and its current stock itself (same owner, `available` status, every material identity field matching the anchor row), and locks exactly the requested count of rows (`for update`, ordered oldest `created_at` first with `id` as a stable tie-breaker) before doing anything else.
- **All-or-nothing**: if fewer than the requested quantity still qualify by the time the lock is acquired (another tab/device got there first), nothing is inserted and nothing is reserved — the RPC raises `cellar_group_stock_changed` ("The available bottle count changed. Please try again."), and the picker's own client-side check separately catches an out-of-range quantity before ever making the request ("Enter a quantity between 1 and {availableCount}.").
- **One transaction, N wines, N reservations**: a single `insert ... select` creates all N `wines` rows (in the session's existing bottle-number/tasting-order allocation scheme) and a single follow-up `update` reserves exactly those N matching `cellar_bottles` rows, each linked 1:1 to the wine it produced via the existing `wines.cellar_bottle_id` column — never a loop of N separate RPC calls from the client, and never a batch that can partially succeed.
- **Feedback and revalidation**: success shows `Bottle added to tasting.` / `{N} bottles added to tasting.` (a new, distinct confirmation screen from the manual-registration flow's unchanged `Registered as Bottle N`), and returning to "Add another bottle" reloads the picker's available list fresh, so grouped counts and filter options immediately reflect what was just reserved.

## Assumptions and design choices

- Price band is no longer part of any new form or scoring calculation (see "Data model notes" above); it was previously required on every bottle.
- Grape/blend is required on every bottle (single variety from the curated list, or a non-empty blend description); the private note remains optional.
- Country and region are required, controlled-vocabulary selections on every new bottle and are no longer free text.
- "Other / Unknown" is offered as a normal region option per relevant country rather than a separate free-text "other region" field — the curated list plus that catch-all option was judged sufficient without adding a second, harder-to-score input path.
- "Confidence" is captured per guess but does not affect scoring, as specified.
- No Supabase service-role key is used anywhere — every privileged operation is enforced by a `SECURITY DEFINER` Postgres function validating a token, which lets the whole app run on the public anon key.
- Host and guest tokens are bearer credentials with no expiry or revocation in this MVP.
- The host control page shows a link to the shared results page after reveal rather than re-rendering the full report inline a second time, and no longer shows any bottle answer-key content itself (previously it did, before other participants could contribute bottles — that's no longer safe to expose to the host).
- Kept the database column name `host_notes` (now used for any contributor's private note, not just the host's) to avoid a wider, riskier rename across the schema, views, and every layer that reads it.
- `entered_by_type` from the brief's suggested schema was left out: `wines.contributor_guest_id` plus `tasting_sessions.host_guest_id` already fully answer "who contributed this bottle, and is it the host" without a redundant column.
- Existing sessions from before this feature keep their original wine labels (e.g. `Wine A`) and are never retroactively given a host participant record; see "Migrating an existing project" in `SUPABASE_SETUP.md`.
- Tasting order uses Move up/down controls rather than drag-and-drop — with typically 3–8 bottles, a couple of taps covers any rearrangement, and this avoids adding a drag-and-drop dependency for marginal benefit.
- A new bottle always joins the tasting order at the end (after whatever the host has already arranged), never inserted mid-sequence automatically.
- Reordering during registration never touches `bottle_number` — only `tasting_order`. Reordering is blocked entirely once the tasting starts (registration closed).
- The blend multi-select is an always-visible, searchable checkbox list rather than a floating popover/combobox — this app has no existing popover primitive suited to an inline dropdown (`Modal.tsx` is a full-screen dialog), and a search-filtered checkbox list is equally accessible without adding a new UI-library dependency.
- The two-grapes-minimum for a blend counts curated picks and unlisted free-text entries *together*, after cross-deduplication — picking "Merlot" and also typing "Merlot" in the unlisted field still counts as one grape, not two, so it can't be used to bypass the minimum.
- `grape_blend_components` is a convenience for re-editing a blend faithfully (which parts were curated picks vs. free text); it is never the source of truth for scoring or display — the flattened `grape_style` text is, exactly as it already was before this feature.
- Guess entry blocks a blend guess with *exactly one* grape at final submission (not before, so autosave never nags mid-draft), matching how a missing rating is already handled; a completely blank blend guess is left alone since it already legitimately scores zero.
- Wine/cuvée guidance is shown as persistent helper text under the field on every device size, rather than a tooltip/info icon, since the app's existing `TextField` already had a `hint` slot wired up for exactly this (accessible via `aria-describedby`) and the copy is short enough not to meaningfully add to page density.
- Tasting mode is locked at creation with no edit path, as specified — changing formats mid-session would leave `revealed_at`/`locked_at` state in an ambiguous position, so this was intentionally out of scope rather than a partial implementation.
- A course_reveal guess is only ever counted as "submitted" for host-progress and reveal purposes once explicitly locked (a new per-guess `wine_guesses.locked_at`) — a rating saved by autosave but never locked in still shows as "no submission" if the host reveals first, matching the requirement that no guess/score is fabricated.
- `guest_visible_wines` and `revealed_wine_guesses` (the views full blind's results page already relies on) are completely unchanged by this feature. Course-by-course reveal reads exclusively through new, narrowly-scoped RPCs (`get_active_bottle_state`, `get_revealed_bottle`) that check `wines.revealed_at` themselves — this was chosen over adding `OR revealed_at is not null` conditions to the shared views, to avoid touching code every other results/registration path depends on.
- The host's "N of M participants submitted" count for the active bottle is refreshed by short polling (every 5s) rather than Supabase Realtime. Realtime's `postgres_changes` broadcasts a full row per RLS policy, independent of column-level grants — so a permissive RLS policy on `wine_guesses` (needed for any realtime signal to reach anon at all) would broadcast guess *content* (ratings, country/region guesses) to every participant in real time, not just a completion count. `wine_guesses` therefore still has no anon grant and no realtime publication membership, exactly as before this feature; only `wines`/`tasting_sessions` changes (already realtime-enabled, never read for their payload content — only used as a "something changed, refetch through a secure RPC" signal) drive live updates.
- A revealed bottle's reveal screen intentionally omits a running leaderboard/ranking (the spec explicitly calls this unnecessary complexity for a mid-tasting screen) — it shows only that bottle's own group stats and per-guest breakdown, reusing `calculateWineResults` scoped to one wine.
- Seen tasting reuses the existing `wine_guesses` table rather than a new `wine_ratings` table — a seen rating and a blind guess are both fundamentally "one row per `(guest, wine)`, with a rating," and the existing table already has every column a seen rating needs (`rating`, `confidence`, `tasting_note`) plus a unique `(guest_id, wine_id)` constraint. A separate table would have meant either duplicating that constraint/index/trigger setup or writing extra joins anywhere both kinds of rows needed to be queried together — for no isolation benefit, since `upsert_seen_rating`/`get_seen_tasting_state` are already mode-gated and never touch the identification-guess columns.
- Seen tasting's report is built by an entirely separate pure-function pipeline (`lib/seenResults.ts`, `SeenBottleResult`/`SeenTastingReport` types) rather than a branch inside `lib/results.ts`/`lib/scoring.ts` — a seen rating has no answer key to score against, so forcing it through `calculateWineResults`/`ScoredGuess` would mean fabricating fake core/bonus scores. The two pipelines share only the generic, scoring-agnostic tie-break helpers (`rankByDescendingKeys`/`maxBy`/`minBy`, now exported from `lib/results.ts`).
- Seen mode's host progress card intentionally has no per-guest "Submitted"/"In progress" status (unlike full_blind/course_reveal's participant list) — there's no meaningful binary state for "rated some but not all bottles," and showing one anyway risked implying more granularity than the aggregate-only requirement intends.
- Like course_reveal's per-active-bottle submission count, seen mode's aggregate rating progress is refreshed by short polling (every 5s) rather than Realtime, for the identical reason: `wine_guesses` has no anon grant or realtime publication membership, since Realtime broadcasts full rows regardless of column grants.
- A seen-tasting rating save is an explicit "Save rating" action, not silent autosave-as-you-type like full_blind/course_reveal's guess entry — the spec's rating-entry actions are `Save rating` / `Back to all bottles`, and an explicit save also makes the exact required confirmation copy ("Rating saved. You can change it until the tasting ends.") meaningful as a one-time event rather than a debounced status label.
- The Tasting Archive's local reference index (`blindCellar.archiveRefs`) is a new, minimal addition rather than reusing a scan of existing `blindCellar.hostToken.*`/`blindCellar.guestToken.*` keys directly — `localStorage` has no efficient prefix-query API, so an explicit small index avoids crawling every key in storage on every archive page load.
- The archive's per-item server response distinguishes "invalid" (token rejected, or a guest token that resolves to a different session than the local reference claimed) from "not_revealed" (a genuinely valid token for a session that just isn't finished yet), so the client can safely forget the first kind of reference and keep the second for later. This is safe to return only because it's always the token's own holder asking about their own reference — it's not a public-facing error message.
- Archive entries never come from a query like "all sessions with status = revealed" — every entry is the result of independently re-validating one locally-held token through the same RPC any other part of the app would use for it. A browser with no local references never triggers a lookup request at all.
- Wine of the Night and blind accuracy in the archive summary are computed from the full `buildTastingReport`/`buildSeenTastingReport` result for each session, the same as the results page — a lighter, partial recomputation was considered and rejected, since duplicating even part of the scoring/ranking logic risked the two surfaces disagreeing over time.
- `get_guest_session_state`'s returned session object gained three additive fields (`id`, `createdAt`, `participantCount`) purely for the archive's own use — no existing caller reads them, so this needed no migration, only a `CREATE OR REPLACE FUNCTION`.
- Email OTP was chosen over magic links because it needs no callback route and no `emailRedirectTo`/redirect-URL Dashboard configuration to function at all — the whole sign-in/verify round trip happens through two direct browser calls (`signInWithOtp`, `verifyOtp`), which is both simpler to implement correctly and has fewer places an open-redirect or misconfigured Site URL could go wrong. See SUPABASE_SETUP.md for how a magic-link flow could be added instead.
- Every existing `anon`-only grant/RPC-execute privilege in `supabase/schema.sql` was widened to `anon, authenticated` *together with* an explicit `revoke all ... from authenticated` on the same four tables `anon` already had revoked — done as one atomic change, not two, specifically because a project's default privileges typically grant `authenticated` broad access no one had a reason to revoke before Auth existed. Widening the grants alone (without also revoking the untouched defaults) would have left a latent path for a signed-in session to read columns like `host_token_hash` that RLS's row-level `using (true)` policies don't restrict.
- Signing in never adds an `auth.uid()` check to any tasting RPC, and no `wines`/`tasting_sessions`/`guests`/`wine_guesses` row references `auth.users` — this phase's account layer is additive and parallel to the token model, not a replacement for any part of it.
- Profile updates (the optional account display name) go straight through Postgres RLS from the browser (`profiles_update_own`, column-limited via `grant update (display_name)`) rather than a Route Handler — this matches how every other participant-owned write in this app already works, and needs no new server code.
- `/account` and `/account/sign-in` protect themselves client-side (check `getUser()`, redirect if absent) rather than via middleware-based route protection — this app has no server-rendered, auth-aware page anywhere, so adding one just for this would be a bigger, riskier change than the two lines of client-side redirect logic it replaces.
- The OTP entry is a single labelled text input (`inputMode="numeric"`, `autoComplete="one-time-code"`, native paste), not six separate digit boxes — this codebase has no existing multi-box input pattern, and a single input is simpler, has one error message instead of managing per-box focus/backspace/paste-splitting, and still gets the same browser/OS one-time-code autofill treatment.
- `claim_account_tasting_record` takes an unambiguous `(p_role, p_token)` pair rather than separate nullable host/guest token parameters — the caller always knows which credential it's presenting (every archive/report entry already carries its own resolved role), so there's no "what if both are provided" case to arbitrate, unlike a two-nullable-token design would have needed.
- The account archive's host-role entries never show a blind-accuracy figure, only participant-role ones do — deriving a host's own guest-row id client-side would need a new `host_guest_id` column grant on `tasting_sessions` purely to recover one summary number, which was judged not worth the extra grant surface. Participant-role accuracy needed no new grant, since `account_tasting_records.participant_id` already gives the guest id directly. This is a deliberate, minor asymmetry, not an oversight — see `resolveAccountRecord` in `lib/archive.ts`.
- `getSupabaseRouteHandlerClient()` (cookie-aware) is a new file, not a change to the existing `getSupabaseServerClient()` (plain anon key, no cookies) that every host-token route already uses — swapping the shared client would have been a much larger, riskier edit than adding one new file used only by the two call sites that actually need to know who's signed in.
- Automatic linking after a signed-in join is fire-and-forget with `keepalive: true`, not awaited before navigating — this matches the explicit requirement that account linking must never add latency or a failure path to joining a tasting.
- "Your record"'s default outer tab is itself (not "This browser"), even when empty, for a signed-in user — the "Your record is waiting" empty state is worth showing proactively rather than only on request, and "This browser" remains one click away either way.
- The Palate Profile's Tasting scope toggle (`profiles.include_seen_tastings`) reuses the exact same column-grant pattern as `profiles.display_name` (`profiles_update_own` RLS + a narrow `grant update`) rather than a Route Handler — a boolean toggle needs no server-side validation beyond "is it true or false," which the column type already enforces.
- `?scope=` on `/profile` is read once on load (if present and valid) and kept in sync via `router.replace` on every toggle, purely for a shareable/bookmarked view of your own profile — it is never treated as a source of truth for persistence (only the toggle handler writes to `profiles`), and it never carries anything beyond the two fixed scope values, so it can't be used to smuggle a different user's data into the page.
- Standout bottles and Most revisited are computed from individual wine *observations*, not deduplicated by wine identity — the same wine tasted at two different evenings correctly appears as two distinct standout-bottle candidates (each with its own date/session), while "Most revisited" separately groups by identity specifically to surface repeats. Building both from one unified, pre-deduplicated list was considered and rejected, since it would have made "tasted the same wine at two different dinners" indistinguishable from "tasted it twice at once."
- A host-only account link whose `host_guest_id` can't be resolved (should not happen in practice, since every session gets one at creation) still counts toward "Tastings attended" and "Recent evenings" but contributes zero bottle/rating/guess observations — hosting is activity in its own right, per the spec's "host-only records still count toward hosted tasting activity."
- The ledger's "Year tasted" filter is the year of the *tasting date*, not the vintage year — vintage is already reachable via search, and a "year tasted" filter answers a genuinely different question ("what did I taste in 2025") that vintage can't.
- `cellar_bottles.region`/`grape_blend`/`vintage` are `not null default ''`/`not null`, matching `wines`' own shape exactly, rather than the nullable columns a first sketch of this schema used — every add/edit RPC still requires them non-blank in practice (same validation as tasting bottle registration), so making them nullable at the column level would only have invited the two tables to drift apart over time for no real flexibility gained.
- `delete_bottle` was made cellar-aware (releasing a linked cellar bottle back to `available` in the same transaction) rather than adding a second "remove this cellar-linked bottle" button to the registration page — the existing Delete flow already does exactly the right thing, including its own authorization (guest-token → contributor match) and its own `registration`-status gate; duplicating that as a new cellar-specific control would have meant re-deriving the same rules in a second place.
- `return_cellar_bottle_to_available` deliberately takes no guest/host token at all — its entire authorization is `auth.uid()` plus `cellar_bottles.owner_user_id`, since it's invoked from `/cellar`, where the signed-in owner may well not be on the browser that holds that session's guest token. This is a different, independently-sufficient authorization chain from the token-based one, not a weaker substitute for it.
- Reservation/return/consumption RPCs use one generic error tag per outcome family (`cellar_bottle_unavailable` covers "not found," "not yours," and "already used" identically) — mirroring `claim_account_tasting_record`'s existing "never reveal which specific check failed" pattern, since a more specific error here could let a caller fish for whether a given cellar bottle id exists or is owned by someone else.
- There is no session-cancellation feature in this app (sessions only ever move `registration → collecting → revealed`), so "return to cellar" is scoped to the `registration` status rather than to a `cancelled` one — this is the existing pre-tasting bottle-removal window, not a new concept introduced for the cellar. If cancellation is ever added, its handler would need to run the same cellar-release step `delete_bottle` already does.
- The cellar selector loads the signed-in user's `available` bottles once (bounded to 300 rows) and filters/searches client-side over that already-owner-scoped, already-bounded set, rather than re-querying Postgres per keystroke — the same "small personal dataset" scaling boundary already documented for the Palate Profile's ledger, appropriate for a personal cellar's realistic size.
- Editing is only ever allowed on `available` bottles, enforced by `update_cellar_bottle`'s own `for update` + status check (never a client-side-only restriction) — reserved/consumed identity is treated as provenance, not a draft, once a physical bottle has been committed to a tasting.
- `delete_cellar_bottle` distinguishes "reserved" from "consumed" in its rejection message, but only ever for the caller's own bottle (the status lookup is scoped to `owner_user_id = auth.uid()`, identically to every other cellar RPC's ownership check) — a bottle that's missing, already deleted, or owned by someone else all collapse to the same generic `cellar_bottle_not_found` tag `update_cellar_bottle` already uses for the same reasons, so a caller can never use the error to probe whether some other user's bottle exists.
- Deletion is enforced by a single conditional `delete ... where ... and status = 'available'` rather than a `select` status-check followed by a separate `delete` — the conditional delete is itself the authorization check, so there is no window in which a concurrent reservation or consumption could race between checking and deleting.
- Grouping is a client-side selector over each screen's existing, already-owner/status-scoped fetch, not a new listing RPC or database view — both `/cellar` and the picker already queried exactly the rows they were allowed to see, so there was nothing for a server-side grouping layer to add beyond re-shaping data the client already legitimately has.
- Multi-bottle Available groups use an expandable row (revealing every underlying physical bottle with its own existing, unmodified Edit/Delete) rather than a "delete one bottle" dialog that names a server-resolved bottle — this needed zero new server-side "which one bottle" resolution logic for Edit/Delete/Return/Consume, and keeps every action targeting a bottle the user can actually see, not one chosen for them.
- Reserved/Consumed grouped rows only show a specific session's title/date/report-link context when the group has exactly one bottle — a multi-bottle Reserved/Consumed group could in principle span more than one tasting, and naming just one of them would be misleading; the generic `Reserved for tasting · N bottles` / `Consumed · N bottles` line is always correct regardless of how many distinct sessions are actually involved.
- Region/Appellation filter ambiguity (the same name shared by more than one country/region) is resolved by disambiguating the dropdown *label* only, never by inventing a composite filter value — the app's curated reference data makes this collision rare in practice, and a plain string value keeps the filter's equality check identical to every other field it compares.
- A one-bottle grouped Add-from-cellar entry still calls the original, unmodified `register_bottle_from_cellar` RPC — only a group of more than one bottle uses the new `register_bottles_from_cellar_group` RPC, so the by-far-most-common single-bottle path carries zero new risk from this feature.
- Cellar "Type" reuses the existing five-value `wine_style` column (Bubbles/White/Red/Sweet/Other) rather than a separate Red/White/Rosé/Sparkling/Orange/Sweet/Fortified/Other vocabulary — the schema has no field capturing that finer classification, and inventing one solely for a filter would be exactly the "fragile duplicate data source" this feature was asked to avoid.

## Recommended next features

- Un-linking a specific account-tasting-record association, and/or deleting the account itself, once there's a clear product need for either
- Claiming/merging across two different signed-in identities (e.g. two accounts for the same person) — deliberately out of scope for this phase, see "Account-linked tasting records"
- Token expiry/revocation and rate limiting on the participant RPC functions, including the archive lookup and account-claim routes
- Partial-credit scoring rules (e.g. close vintage, correct country but wrong region)
- Proper Postgres migrations instead of a single `schema.sql` for schema evolution
- An external wine-data API for country/region/grape lookups, if the curated static list ever feels limiting
- True cursor/offset database-level pagination for the Tasted Wines Ledger, once a user's account-linked history is large enough that in-memory pagination over their own sessions stops being cheap
- Un-linking a specific tasting from the Palate Profile independent of un-linking it from the archive entirely
- Cross-session blind-accuracy trends over time (e.g. "improving" vs. a single all-time figure), deliberately left out of this phase's Blind palate section to avoid implying statistical rigor a small personal sample doesn't support
- Cellar bottle price/purchase-price tracking, market valuations, or Wine-Searcher-style external enrichment
- Bulk/CSV cellar import, barcode scanning, or label OCR
- Bottle photos
- Shared/household cellars, or any cellar access for a second signed-in user
- A "replace this cellar bottle with a different one" swap flow for an already-reserved tasting bottle — deliberately deferred; today the only supported change before tasting begins is return-to-cellar followed by a fresh selection
- True tasting-session cancellation (with its own cellar-release step), if this app ever adds one

## Manual test checklist

Run this against a real Supabase project (see `SUPABASE_SETUP.md`) using multiple devices/browsers (or one device + incognito windows to simulate other participants):

1. **Host creates a registration-stage session.** Go to `/host/new`, fill in title/date/your display name, submit. You land on `/host/[publicId]?token=...` showing "Bottle registration open", the join code, QR code, and copy-link/copy-code buttons.
2. **Host contributes zero or multiple bottles.** From the host page, click "Register my bottle" to add one or more bottles via `/register/[publicId]`; confirm "Start tasting" stays disabled with 0 bottles and enables once at least 1 exists — and confirm the host can also legitimately contribute 0 bottles and still start tasting once someone else has registered one.
3. **Multiple participants join on different phones/browsers** via the QR code or join link, landing on `/register/[publicId]`.
4. **One participant adds several bottles** — confirm the registration home lists all of them with sequential numbers, and "Add another bottle" is offered after the first.
5. **Different participants add bottles at the same time** (fire two registrations within a second or two of each other from different devices) — confirm no two bottles in the session ever get the same number.
6. **Bottle numbers remain sequential and unique**, and **deleted numbers are not reused** — delete a middle-numbered bottle, then register a new one, and confirm the new bottle's number is higher than every number ever issued, not a reused gap.
7. **A participant cannot see another contributor's answer key** — inspect the registration home and network responses; only your own bottles' details should ever appear.
8. **Participants can edit/delete only their own bottles before tasting starts** — confirm attempting to edit/delete via a manipulated URL for someone else's bottle fails.
9. **Host starts tasting and locks registration** — confirm the confirmation modal text, that bottle registration closes, and that participants on `/register` automatically transition to `/tasting` via Realtime.
10. **Host submits guesses as a participant** via "Enter my guesses" on the host page, using the same guess-entry flow as anyone else, including guessing their own contributed bottle.
11. **Guests submit guesses** for every bottle, autosaving as they go, then submit and see the "locked" screen.
12. **Host reveals wines** via the confirmation modal on the host page.
13. **Contributors are visible only after reveal** — before reveal, no contributor name ever appears anywhere; after reveal, every bottle's card shows "Contributed by [name]".
14. **Host appears exactly once in the leaderboard**, with their own bottle (if any) scored identically to everyone else's — no bonus or penalty for contributing.
15. **Existing pre-feature sessions still load correctly** — a session created before this migration (status `collecting` or `revealed`, wines labeled `Wine A`/`Wine B`) continues to work end-to-end without a host participant record.
16. **Country/region controls work correctly** in both bottle registration and guess entry — region is disabled with a "Select country first" placeholder until a country is chosen, only shows that country's regions, and resets to blank if you change country after picking a region.
17. **Vintage picker works correctly** — toggling to "NV" hides the year selector and stores `NV`; toggling back to "Vintage year" clears any prior NV value and requires a fresh year pick; years list current+1 down to 1900, descending.
18. **Grape/blend selector works correctly** — "Single variety" shows a dropdown (required for registration); "Blend" shows the multi-select grape picker plus an optional "Other grape(s)" field (see "Structured grape/blend" checklist below for detail); switching modes clears the previous value; a blend entered with the same grapes picked in a different order (e.g. Merlot then Cabernet Sauvignon vs. Cabernet Sauvignon then Merlot) still scores as correct after reveal.
19. **Price band appears nowhere** in the new bottle form, edit form, guess form, or results/leaderboard.
20. **Producer and wine/cuvée are labelled "Producer, optional" / "Wine / cuvée, optional"** in guess entry (not "bonus" — see README "Scoring model"); for a `legacy_v1` session the results view separates core categories (100 pts) from bonus categories (20 pts) with a `Core: X/100 · Bonus: Y/20 · Total: Z/120` summary per guess, while a `core_v3_appellation_conditional` session shows a single `Total: Z/80` or `Total: Z/100` summary with Producer/Wine-cuvée as an unscored "Personal precision calls" comparison instead.
21. **Existing sessions with legacy grape/style and price-band data still load and score sensibly** — a bottle registered before this update (no `grape_blend_mode`) still displays its old grape/style text and scores against new-format guesses using the alias-aware text fallback described in "Scoring model" above.

### Wine style

22. **Wine style is required to register or edit a bottle** — submitting without a style is rejected client-side; picking each of Bubbles/White/Red/Sweet/Other is accepted.
23. **Legacy bottles show "Other"** — a bottle registered before this feature displays wine style "Other" everywhere it's shown.
24. **A contributor can edit their own bottle's style during registration only** — editing after tasting starts is unavailable, matching every other bottle field.
25. **The host sees style + anonymous number only** — on the host page's tasting-order list, confirm no country/region/producer/vintage/notes/contributor-name ever appears, for any bottle, before reveal.
26. **Other participants cannot see any bottle's style pre-reveal** — inspect the registration and guess-entry pages/network responses for a non-host, non-contributor guest; wine style never appears there before reveal.
27. **Wine style appears in revealed results** as `Style: <Style>` on every bottle's result card.

### Tasting order

28. **Bottle number and tasting order are independent** — reorder a session's bottles and confirm every bottle keeps its original `Bottle N` label throughout.
29. **Reordering only ever changes `tasting_order`** — verify via the database (or by re-registering the same bottle) that `bottle_number` is untouched by a reorder.
30. **Tasting order stays a contiguous 1..N permutation** — after any sequence of moves, every bottle in the session has a unique order value with no gaps.
31. **A newly registered bottle joins at the end of the tasting order**, not inserted into the middle of an already-arranged sequence.
32. **Deleting a bottle doesn't reuse its bottle number** and leaves the remaining bottles' tasting order still a valid, gapless 1..N sequence.
33. **Reordering is host-only and registration-only** — a non-host request is rejected; a reorder attempted after tasting has started is rejected with the tasting order left unchanged.
34. **An invalid or duplicate reorder payload is rejected** — submitting a list that omits a bottle, repeats an id, or includes an id from another session fails without partially applying.
35. **Guess entry follows the host's tasting order**, not bottle-number order, once collecting starts — confirm the progress label (`Bottle 4 — 1 of 5`) and Previous/Next sequence match the host's arranged order.
36. **Results show both the bottle number and a served-order indicator** (e.g. "Served 1st") for every bottle.
37. **Starting the tasting locks the order** — confirm the "Start tasting" confirmation mentions the tasting order will be locked, and that the host's reorder controls disappear/reject changes once collecting begins.

### Structured grape/blend

38. **Single variety is unchanged** — the dropdown still allows exactly one selection and scores exactly as before.
39. **Blend mode shows the multi-select** with a search box, checkboxes, and an "Other grape(s)" free-text field beneath it.
40. **Picking two or more curated grapes saves and re-edits correctly** — reopen the bottle's edit form afterwards and confirm the same checkboxes are still checked.
41. **Picking exactly one grape is rejected** — inline message reads exactly "Select at least two grapes for a blend, or choose Single variety instead," both in bottle registration/edit and (at final submit only, not mid-draft) in guess entry.
42. **Checking a grape already typed in "Other grapes" doesn't double-count it** — total-grape validation still requires two *distinct* grapes.
43. **Typing multiple unlisted grapes** (e.g. "Carignan, Counoise") separated by commas/slashes/semicolons/ampersands/line breaks each become a separate component; duplicates (including case-insensitive ones) collapse to one.
44. **A blend using only unlisted grapes** (zero curated picks, two-plus typed) saves and validates correctly.
45. **Guessing the identical blend with grapes selected in a different order** still scores full 30 points after reveal.
46. **Typing "Shiraz" as an unlisted grape** while the answer has "Syrah" (or vice versa, via the curated picker) still scores as a match.
47. **A guess missing one component of a 3-grape answer blend** scores zero for grape/blend, not partial credit.
48. **A legacy blend bottle (registered before this feature, plain flattened text)** still loads, still scores correctly against new-format guesses, and its edit form pre-populates the multi-select with whichever of its grapes are recognised, without altering the stored answer unless explicitly re-saved.
49. **No pre-reveal secrecy leak** — inspect network responses for a non-contributor, non-host participant before reveal; no bottle's `grape_blend_components` or any other grape/blend detail is ever visible.

### Wine/cuvée guidance

50. **The exact helper text** ("Enter the wine name or cuvée.") appears under Wine / cuvée on the add-bottle form, the edit-bottle form, and the guess-entry form (labelled "Wine / cuvée, optional" there) — no mention of cru or appellation, since Appellation is now its own dedicated field.
51. **The helper text is programmatically associated with the field** (inspect `aria-describedby` on the input pointing at the hint's `id`), not just visually nearby.
52. **Wine/cuvée bonus scoring is unchanged** — still worth 10 points, still an exact normalised-text match, unaffected by the new helper text.

### Tasting modes

53. **Creating a session defaults to Full blind tasting** and shows both mode cards with their exact descriptions before creating.
54. **Choosing Course-by-course reveal** persists that mode; reopening the host page afterwards shows the mode label + description and no way to change it.
55. **A full blind session behaves exactly as before**: no per-bottle reveal control anywhere, all bottles hidden during collecting, one final reveal shows the complete report.
56. **A course_reveal session only shows one active bottle** to participants at a time — attempting to reach a future bottle's guess form (e.g. by guessing at a wine id from the tasting order) is rejected server-side.
57. **The host can reveal the active bottle before every participant has submitted** — the confirmation modal names the bottle and warns non-submitters get no score; after reveal, a participant who never locked a guess shows no score for that bottle, not a fabricated one.
58. **The host cannot reveal a bottle out of order** — attempting to reveal a later bottle while an earlier one is still unrevealed is rejected server-side (`bottle_not_active`), and revealing an already-revealed bottle is rejected (`bottle_already_revealed`).
59. **A non-host cannot reveal any bottle** — calling the reveal endpoint with a guest token (or no token) fails.
60. **Revealing a non-final bottle** keeps the session `collecting`, makes the next unrevealed bottle active, and shows the per-bottle reveal screen (answer key, contributor, group stats, every locked guess's score breakdown) — while the *next* bottle's identity/guesses remain completely invisible.
61. **Choosing "Continue to next bottle"** loads the new active bottle's empty guess form; reloading the reveal screen later still works, and reloading the active-bottle route routes to whatever's current (next active bottle, or final results if the session finished while away).
62. **Revealing the final bottle** transitions the session to `revealed` and shows the same comprehensive final report (Wine of the Night, Best Taster, Most Divisive Wine, full leaderboard, every bottle) as full blind.
63. **The host can submit their own guess** for the active bottle in course_reveal, same as any participant.
64. **Bottle numbers and tasting order never change** across any of the above — only `revealed_at`/`locked_at` timestamps move.
65. **Two devices see live updates**: a second participant's screen transitions from "locked, waiting" to the reveal prompt shortly after the host reveals, without needing a manual refresh (allow a few seconds — see the polling/realtime note in "Assumptions and design choices").
66. **A pre-existing session from before this feature** still loads and behaves as `full_blind` with no visible change.

### Seen tasting

67. **Creating a session shows all three mode cards** (Full blind tasting, Course-by-course reveal, Seen tasting) with their exact descriptions, defaulting to Full blind tasting; choosing Seen tasting persists that mode with no way to change it afterwards.
68. **Participants register bottles exactly as in any other mode** — normal pre-start secrecy applies (no contributor sees another's bottle before the host starts tasting).
69. **The moment the host starts tasting, every participant immediately sees every bottle's full details** (style, country, region, grape/blend, vintage, producer, wine/cuvée, contributor) at `/session/[publicId]/seen` — inspect network responses for a non-host, non-contributor participant to confirm nothing is masked.
70. **A participant can rate bottle 1, skip to bottle 4, then return to bottle 1 and change its rating** — all while the session stays `collecting`; tasting order is display order only, never a pacing gate.
71. **Re-saving a rating for the same bottle updates the existing row rather than creating a duplicate** — confirm via the database (or by reloading the rating page and seeing the latest value, not the first one).
72. **Other participants cannot see that rating while the tasting remains open** — inspect network responses for a second participant; only their own `myRating`/`myConfidence`/`myNote` ever appears, never anyone else's.
73. **The host sees aggregate progress only** — "N of M participants have rated at least one bottle" and "N of M possible ratings entered," never an individual participant's rating or note, and no average/ranking before ending.
74. **A late-joining participant** (joins after the host starts tasting) lands on the same seen-tasting list, sees every bottle, and can rate any or all of them; joining after the host ends the tasting instead routes straight to results with no rating controls.
75. **The host ends the tasting** via "End tasting and reveal ratings," confirming the exact modal copy ("End seen tasting?" / the locking warning / "Cancel" / "End tasting and reveal results").
76. **Ratings become locked the instant the session is revealed** — a save attempt still in flight (or retried) at that point is rejected server-side with a clear error, and the rating/confidence/note fields already saved are exactly what's shown in the final report, nothing more.
77. **Final results show the rating ranking, Wine of the Night, and Most Divisive Wine, with no Best Taster, scoring, or identification-accuracy content anywhere on the page.**
78. **A bottle nobody rated shows `averageRating: —` and an empty ratings list** (or every participant showing "No rating"), never a fabricated average or score.
79. **Full blind and course-by-course sessions are unaffected** — create one of each alongside a seen session and confirm both behave exactly as described in their own sections above, with no shared-state leakage between them.
80. **A pre-existing full_blind or course_reveal session still loads and behaves exactly as before** — this feature adds a third allowed `tasting_mode` value without touching the check constraint's existing two options' behaviour.

### Tasting archive

81. **A host creates and completes a full_blind (or any mode) tasting on one browser.** After reveal, visiting `/archive` on that same browser shows it under "Hosted by you" with the correct title, date, mode, bottle count, participant count, and Wine of the Night (or "No group rating recorded" if nothing was rated).
82. **A participant joins and completes that tasting on a different browser/incognito window.** After reveal, `/archive` on the participant's browser shows it under "Joined by you" — and the host's own browser never shows a "Joined by you" duplicate for the same session (it only appears under "Hosted by you" there).
83. **A session still in registration or collecting never appears in either archive tab**, even though a valid token already exists locally for it — revisit `/archive` before and after the host reveals to confirm it appears only afterward.
84. **Clearing/corrupting the `blindCellar.archiveRefs` localStorage key, or a session's host/guest token, removes that session from the archive** on next load without affecting any other stored session's tokens.
85. **"View report" from an archive entry opens `/results/[publicId]?from=archive`**, showing the exact same mode-specific report as visiting the plain results link directly, plus a "FROM THE ARCHIVE" marker and a working "Back to archive" link.
86. **A seen-tasting archive entry and its opened report never show Best Taster or a blind-accuracy figure anywhere**, and the archive summary for a tab containing only seen-tasting entries never shows a "Blind tasting accuracy" figure.
87. **The empty states' actions work**: "Host a tasting" from the empty "Hosted by you" tab reaches `/host/new`; "Join a tasting" from the empty "Joined by you" tab reaches `/join`.

### Accounts

88. **Every anonymous workflow above still works with no sign-in at all** — re-run a few of the checks above (host creation, joining, registering a bottle, guessing, revealing) while signed out; nothing should differ from before this feature existed.
89. **Sending a sign-in code**: on `/account/sign-in`, an invalid email shows a client-side error and never calls Supabase; a valid email shows "A sign-in code has been sent to your email." and moves to the code step, regardless of whether that address has signed in before (no "this email doesn't have an account" type of message ever appears).
90. **Verifying the code**: entering the real 6-digit code from the email (via typing or pasting) redirects to `/account` (or the `redirect` target); entering a wrong or expired code shows "That code is invalid or has expired. Request a new code and try again." without exposing any Supabase error detail.
91. **Resend cooldown**: "Send a new code" is disabled with a visible countdown immediately after a code is sent, and works again once the cooldown ends.
92. **Signing in on a second browser/device** with the same email reaches the same account (same email shown on `/account`), but does **not** show that browser's own `/archive` any sessions from the first device — the archive stays browser-linked, not account-linked, in this phase.
93. **The account display name is independent of tasting display names**: setting a name on `/account` never changes the name already used in an existing joined session, and joining a new tasting still prompts for its own display name regardless of the account name.
94. **An unsafe `redirect` query value is ignored**: visiting `/account/sign-in?redirect=https://example.com` and completing sign-in lands on `/account`, never on the external URL.
95. **Sign out** returns to Home (with a brief "You have signed out." confirmation), and immediately afterward this same browser can still host a new tasting, join a tasting, and see its existing `/archive` entries exactly as before signing in.
96. **A signed-in browser can still do everything an anonymous one can** — host a tasting, join one, register a bottle, submit guesses/ratings, and view a revealed report — confirming the `authenticated`-role grants added alongside Auth didn't narrow anything the app already relied on.
97. **A signed-in user gains no extra access**: knowing you're signed in (with any email) never lets you open another person's host controls, another session's report before it's revealed, or another user's `/account` data — only a valid host/guest token (unchanged) and, for `/account`, only your own session's `auth.uid()` grant that.

### Account-linked tasting records

98. **A signed-in host creates a tasting.** No visible change to the create flow; after reveal, `/archive` → "Your record" shows it on that same browser with no extra action, and — the real test — signing in with the same email on a second browser/device also shows it there.
99. **A signed-in participant joins a tasting.** No visible change to the join flow (including display-name prompt); after reveal, "Your record" shows it on a second signed-in device too, correctly labeled "Participant."
100. **A signed-in host who also joins their own session as a participant** sees the session exactly once in "Your record," labeled "Host," not twice.
101. **Repeatedly reloading/retrying a create or join while signed in never produces duplicate account records** — re-check "Your record" shows the session exactly once regardless of retries.
102. **Historic claim, hosted session**: on the original browser, signed in, open a revealed hosted session in "This browser" → click "Add to my tasting record" → see "Added to your record" → it now also appears in "Your record" on that browser and on a second device.
103. **Historic claim, participant session**: same as above for a joined session — claim succeeds, appears correctly labeled "Participant" afterward, and the claimed participant identity still cannot open host controls anywhere.
104. **Claim fails safely and quietly** when tried against a still-collecting/registration session (no claim action shown at all), an invalid/stale local token (clear localStorage for that session first), or while signed out (no claim action shown) — in every case, existing browser/report access is completely unaffected afterward.
105. **An unclaimed browser-only historic session never appears on a second device** — sign in with the same email on a different browser and confirm "Your record" does not include it, only "This browser" (on the original browser) does.
106. **Already-linked entries never show a redundant claim action** — after claiming, revisit "This browser" and confirm that entry now shows "Already in your record" instead of the claim panel.
107. **Full blind, course-by-course, and Seen reports remain correct** when opened via "Your record" (`FROM YOUR RECORD` / "Back to your record") exactly as they are via "This browser" (`FROM THE ARCHIVE` / "Back to archive") or a direct link — same scores, same Best Taster/no-Best-Taster rules, same privacy.
108. **The empty "Your record" state** (`Your record is waiting.`) appears for a signed-in user with zero account-linked revealed sessions, with working "Return home" and "View this browser's archive" actions.

### Palate Profile

109. **A signed-out visitor to `/profile`** is redirected to `/account/sign-in?redirect=/profile` and lands back on `/profile` after signing in.
110. **A signed-in user with no account-linked revealed sessions** sees calm empty states throughout — At a glance shows zero/`No ratings recorded`, Blind palate shows "Your blind record will take shape after your first completed blind tasting.", the ledger shows "Your wine ledger is waiting." with a working "View tasting archive" action.
111. **Default scope is `Blind tastings only`** on first visit for a user who has never changed the preference.
112. **Switching to `Include Seen tastings`** changes At a glance, Your wine record, the ledger's results, and Recent evenings, but leaves every Blind palate figure (blind accuracy, category rows, calls submitted) completely unchanged.
113. **The scope choice persists** — reload the page, and sign in on a second browser/device, and the same scope is selected both times.
114. **A signed-in host who also joined their own session** appears once in Recent evenings and in "Tastings attended," labeled correctly (Host, Participant, or Host and participant), never twice.
115. **A wine tasted at two different evenings** appears twice in the ledger (once per evening) but counts once in "Unique wines tasted" and appears in "Most revisited" with 2 occasions.
116. **Search, filters, and sort on the ledger** each narrow/reorder results correctly, and changing any of them (including the scope toggle) resets to page 1.
117. **A long producer/cuvée name** wraps cleanly on a narrow mobile viewport with no horizontal scrolling, in both the ledger and Your wine record.
118. **This browser's other account's/another account's data never appears** — sign in with a different email and confirm the profile, ledger, and blind palate are all empty (or show only that account's own activity), never the first account's.
119. **Report links from the profile** (`View report`, `?from=account`) open the correct session with the `FROM YOUR RECORD` banner, respecting the same access rules as opening it from the archive.
120. **Existing tasting modes, reports, archive, and account behavior are all unaffected** by this feature — re-run a handful of checks from the sections above.

### Personal Cellar

121. **A signed-out visitor cannot reach `/cellar`** — redirected to `/account/sign-in?redirect=/cellar`, lands back on `/cellar` after signing in.
122. **A signed-in user adds a bottle** via `/cellar/new` — lands back on `/cellar` with "Bottle added to your cellar." and the new row under the Available tab.
123. **A signed-out guest registering a bottle sees only the existing manual flow** — no cellar choice, no cellar data, registration works exactly as before this feature.
124. **A signed-in guest with at least one available cellar bottle sees the "Add a new bottle" / "Add from my cellar" choice**, defaulting to the existing manual flow — nothing is force-selected.
125. **Choosing "Add from my cellar" and selecting a bottle** shows the read-only confirmation panel with the correct wine identity, format, and the exact required reservation notice; "Choose another bottle" returns to the selector, "Add a new bottle instead" falls back to the manual form.
126. **Confirming "Use this bottle"** registers the tasting bottle with the cellar bottle's wine details, and that cellar bottle now shows as `Reserved` on `/cellar`, linked to the correct tasting.
127. **The same cellar bottle cannot be selected again** — it no longer appears in the selector or in the Available tab.
128. **A signed-in user with zero available cellar bottles** sees the existing manual flow by default, with the exact required note, and is never blocked from registering manually.
129. **The host and other participants never see cellar provenance** — inspect the host's bottle list, another participant's guess screens, and the final report; no storage location, personal note, or "from a cellar" indicator ever appears anywhere outside the owner's own `/cellar`.
130. **Owner returns a reserved bottle to the cellar before the tasting starts** — either by deleting it from the registration page (existing "Delete" flow) or via "Return to cellar" on `/cellar`'s Reserved tab — in both cases the bottle becomes `Available` again and the tasting bottle is gone from the session.
131. **Return is not offered/does not succeed once the session has left `registration` status** (collecting or revealed) — the Reserved tab shows no "Return to cellar" action once that threshold has passed.
132. **Owner marks a reserved bottle "Mark as consumed" after the tasting is revealed** — confirmation dialog shows the exact required copy; on success the bottle moves to the Consumed tab with a consumption date and a working "View tasting report" link.
133. **"Mark as consumed" is not available before reveal** — no such action appears on a Reserved bottle whose tasting hasn't been revealed yet.
134. **A consumed bottle remains in history, cannot be re-selected, and cannot be edited** — visiting its (non-existent) edit route or attempting to reuse it in a new registration is not possible from the UI, and the bottle stays visible under Consumed indefinitely.
135. **Editing an available cellar bottle** via `/cellar/[bottleId]/edit` works and reuses the same validation as adding one; attempting to reach the edit route for a reserved or consumed bottle shows the exact required read-only message instead of a form.
136. **Full blind, course-by-course reveal, and Seen tasting workflows all still behave correctly** for both manually-registered and cellar-registered bottles — same scoring, same reveal timing, same report content.
137. **Desktop and mobile `/cellar`, `/cellar/new`, and the registration page's cellar selector** all work with no horizontal overflow, ~44px touch targets, and status conveyed by text (not color alone).
138. **Add a Burgundy wine and select Chablis** — on any bottle-entry form (participant Add/Edit, cellar Add/Edit), pick France → Burgundy and confirm the Appellation field appears with the full curated Burgundy list, including Chablis; select it and save.
139. **Add a Piedmont wine and select Barolo** — pick Italy → Piedmont and confirm the Appellation field appears with Barolo/Barbaresco among its options; select Barolo and save.
140. **Choose an unsupported region and confirm Appellation stays hidden** — pick any country/region pair not in the curated list (e.g. France → Corsica, or any country before a region is chosen) and confirm no Appellation field, disabled input, or "No appellations available" message ever appears.
141. **Changing region clears Appellation** — with a supported region and appellation selected, change the region (or country) and confirm the appellation resets to blank, the field hides/reappears correctly for the new pair, and an accessible "Appellation cleared because the region changed." announcement fires.
142. **Save with no Appellation** — on a supported country/region pair, leave Appellation blank and confirm the bottle saves normally with no validation error.
143. **Edit an existing historic bottle with no Appellation** — open the edit form for a bottle registered before this feature; confirm it loads with Appellation blank (not an error) and saves normally whether or not one is added.
144. **Confirm blind screens never leak Appellation** — before reveal, inspect every participant/host view and network response for a bottle with a saved appellation; it must never appear anywhere pre-reveal, identically to region/country.
145. **Confirm the revealed report shows Appellation correctly** — after reveal, the course-reveal per-bottle screen shows Appellation as its own row when present and omits the row entirely when null; the final report and ledger show it via the compact `Appellation, Region, Country` string.
146. **Confirm mobile dropdown usability for Appellation** — on a narrow viewport, the Appellation select has no horizontal overflow, a ~44px touch target, and the exact required helper text ("Optional. Select the recognised appellation if applicable.") when visible.

### Scoring model replacement

147. **A new session scores a wine with an Appellation out of 100** — register a bottle with a supported country/region and pick an Appellation; guess it correctly along with everything else; after reveal, confirm the per-guess total reads `100/100` with Appellation shown as a normal scored row.
148. **A new session scores a wine without an Appellation out of 80** — register a bottle leaving Appellation blank (or in an unsupported country/region); guess everything else correctly; after reveal, confirm the total reads `80/80` and the Appellation row reads "Not applicable", never `0/20`.
149. **Leaderboard percentage ranking is fair** — in one session with a mix of Appellation and non-Appellation wines, confirm a taster who only guessed 80-point wines perfectly ranks above one who scored 90/100 on a 100-point wine, and that the leaderboard displays both a percentage and a raw `X / Y points` figure per taster.
150. **Producer/wine-cuvée never affect a new session's score** — fill in a wrong Producer and Wine/cuvée on an otherwise-perfect guess; confirm the total is unaffected, the fields are labelled "Producer, optional" / "Wine / cuvée, optional" (not "bonus") in the guess form, and appear only as an unscored "Personal precision calls" comparison at reveal.
151. **A historic (pre-migration) session's report is completely unchanged** — open the report for a session created before this feature; confirm it still shows the 100-core/20-bonus/120-total breakdown exactly as before, ranked by raw points, with no "Not applicable" language or percentage-based ranking anywhere.
152. **Course-by-course reveal matches the final report's scoring** — in a course-reveal session, confirm the per-bottle reveal screen and the eventual final report agree on every guess's score, denominator, and Appellation applicability.
153. **Seen tasting is completely unaffected** — confirm a Seen session still has no guesses, no scores, no leaderboard, and no Best Taster anywhere, exactly as before this feature.
154. **Palate Profile blends old and new sessions correctly** — for an account with at least one historic and one new-model completed blind tasting, confirm "Blind accuracy" and the raw points basis look sane (no impossible percentages), "Bonus precision" no longer appears anywhere, and the Appellation category row only reflects new-model wines that actually had one.

### Cellar quantity

155. **Add one bottle** (leave Quantity at its default 1) — confirm behavior is identical to before this feature: one new `Available` row, "Bottle added to your cellar." confirmation.
156. **Add six identical bottles** — set Quantity to 6 on `/cellar/new`; confirm the confirmation reads "6 bottles added to your cellar." and `/cellar`'s Available tab immediately shows six separate rows with identical wine/format details.
157. **Reserve one of six without affecting the others** — register one of the six bottles for a tasting; confirm exactly one moves to Reserved and the remaining five stay Available.
158. **Consume/return one of a batch independently** — mark one reserved duplicate as consumed (or return it to available); confirm the other duplicates are completely unaffected.
159. **Invalid quantity values are rejected client-side** — try `0`, a negative number, a decimal (e.g. `2.5`), and `101`; confirm each shows the exact field error "Enter a quantity between 1 and 100." and does not submit.
160. **Quantity does not appear on the edit form** — open "Edit" on an existing Available bottle and confirm there is no Quantity field; editing only ever changes that one bottle.
161. **Other users cannot view or add to someone else's cellar** — signed in as a second account, confirm `/cellar` shows only that account's own bottles, and there is no way to target another user's `owner_user_id` from the add form.
162. **Mobile usability** — at a narrow viewport, confirm the Quantity input has no horizontal overflow, a comfortable touch target, a numeric keyboard on mobile, and correct label/hint/error association for screen readers.

### Other grape

163. **Register a tasting bottle with a listed grape** — confirm behavior is unchanged: no custom text input appears, and the bottle saves with the curated grape name.
164. **Register a tasting bottle with Other grape** — select "Other grape" in Single variety mode, confirm the "Grape variety" text input with hint "Enter the grape variety." appears directly below the dropdown, type a custom name (e.g. "Mondeuse Blanche"), and confirm the bottle registers successfully.
165. **Add a cellar bottle with Other grape** — same as above on `/cellar/new`; confirm the wine-identity display elsewhere in the cellar list shows the custom grape name naturally, with no special formatting.
166. **Edit an available cellar bottle with a custom grape** — reopen its edit form; confirm the selector shows "Other grape" (not blank, not the nearest curated grape) with the stored custom text populated in the text field, and that saving without changes doesn't alter the stored value.
167. **Switch between dropdown and Other grape repeatedly** — pick a listed grape, switch to Other grape, type text, switch back to a listed grape, confirm the custom text input hides and its value is discarded (not silently resubmitted); switch to Blend and back to Single variety, confirm the custom text is cleared, not stale.
168. **Enter Other grape in a Full-blind guess** — confirm the same dropdown/text-input behavior as registration; submit a custom guess and confirm it saves.
169. **Save and restore a guess draft with Other grape** (course-reveal) — enter a custom grape guess, navigate away before locking, come back; confirm the selector still shows "Other grape" with the typed text restored, not reset to blank.
170. **Test custom actual/guess scoring match** — register a bottle with a custom grape (e.g. "Mondeuse Blanche") and guess the exact same custom text; after reveal, confirm the grape/blend field scores correct. Guess a different custom text (e.g. "Trousseau") on a second attempt and confirm it scores incorrect, not a crash or an automatic pass.
171. **Test invalid multi-grape text** — in the Other-grape text field, type "Cabernet Sauvignon / Merlot"; confirm the exact field error "Use Blend for more than one grape." appears and the form does not submit. Try a value over 80 characters or with digits/symbols and confirm "Enter a valid grape variety." appears instead.
172. **Confirm mobile and keyboard behavior** — at a narrow viewport, confirm the dropdown and the conditional text input have no horizontal overflow and comfortable touch targets; using only the keyboard, confirm the dropdown and text input are both reachable in a sensible tab order, focus is never moved unexpectedly when the text input appears, and both fields have their own visible, correctly associated label.

### Seen Host Controls

173. **Host Controls shows wine names, not bottle labels** — create a Seen tasting with several wines, start it; confirm each row shows `Producer — Wine/cuvée` as the primary line and `Vintage/NV · Appellation, Region · Country` (values omitted cleanly when absent) as the secondary line, with the bottle's existing anonymous label kept only as small context above it.
174. **Full blind and course-reveal Host Controls are unchanged** — confirm both still show bottle numbers/labels only during collecting, with no wine identity, no rating-status text, and no reveal-ratings control anywhere.
175. **Rating status updates live** — with the tasting started, submit ratings from a second participant device/tab; confirm the host's `{ratedCount} of {eligibleCount} rated` line for that bottle updates within the existing poll interval, without a page reload.
176. **Reveal a later bottle before an earlier one** — with several unrated/partially-rated bottles, reveal e.g. the third bottle's ratings first; confirm only that bottle shows `Ratings revealed` and a group rating, every other bottle is unaffected, and the action succeeds even though not every participant has rated it yet.
177. **Reveal confirmation dialog** — confirm it reads exactly "Reveal ratings for this wine?" / "Participants will be able to see the group rating for this wine.", is keyboard accessible (Escape cancels, focus starts on the dialog), and Cancel leaves the bottle unrevealed.
178. **Participant sees only the revealed bottle's group rating** — as a participant, confirm the revealed bottle now shows "Group rating: …" on the list and detail pages, while every other, still-unrevealed bottle shows no group rating at all, even if you already rated it.
179. **Rating edits are locked for a revealed bottle** — as a participant who already rated the revealed bottle, open its rating page; confirm the rating slider, confidence picker, note field, and Save button are disabled/hidden with a "ratings for it are now locked" message, and that a direct save attempt (e.g. a stale open tab) is rejected server-side.
180. **Unrevealed bottles remain fully ratable** — confirm a participant can still freely submit/edit ratings for every bottle the host hasn't revealed yet, exactly as before this feature.
181. **Reveal another bottle afterward, in any order** — reveal a second, then a third bottle in non-sequential order; confirm each reveal is independent and none of it requires ending the tasting.
182. **End the tasting and confirm the final report** — click "End tasting and reveal ratings"; confirm the final report shows every bottle's full result set (including ones never individually revealed during collecting), exactly as this report behaved before this feature.
183. **Mobile layout** — at a narrow viewport, confirm each Host Controls row stacks wine identity, rating status, and the Reveal ratings button cleanly with no horizontal overflow and a ~44px touch target on the button.

### Cellar deletion eligibility

184. **Delete an Available bottle** — on `/cellar`'s Available tab, confirm the row shows Edit (pencil) and Delete (trash) icon buttons beside the bottle name; click Delete, confirm the dialog reads exactly "Delete this bottle?" / "This will permanently remove it from your cellar." with "Delete bottle" / "Cancel"; confirm Delete bottle removes the row and shows "Bottle deleted from your cellar."
185. **Reserved and Consumed rows show no Edit/Delete icons** — switch to the Reserved and Consumed tabs; confirm neither icon (nor a disabled icon, nor any reserved empty space beside the name) appears on any row, and the bottle name stays aligned the same way it does on Available rows.
186. **Direct RPC attempt against a Reserved bottle is rejected** — call `delete_cellar_bottle` for a bottle you know is Reserved (e.g. via the browser console with your signed-in session); confirm it fails with the exact message "This bottle cannot be deleted while it is reserved." and the bottle's reservation, tasting link, and session data are completely unaffected afterward.
187. **Direct RPC attempt against a Consumed bottle is rejected** — same as above for a Consumed bottle; confirm the exact message "Consumed bottles remain part of your cellar record." and that its cellar history, linked tasting report, and Palate Profile data are unaffected.
188. **Returning a Reserved bottle makes it deletable again** — use "Return to cellar" on a Reserved bottle, confirm it becomes Available with working Edit/Delete icons, then successfully delete it.
189. **A Consumed bottle can never become deletable** — confirm there is no action anywhere that returns a Consumed bottle to Available, and its Delete icon never appears under any circumstance.
190. **Double-clicking Delete is safe** — open the delete confirmation, click "Delete bottle" and confirm the button disables immediately (reads "Deleting…") so a second click can't fire a duplicate request.
191. **Another user's Available bottle can't be deleted** — confirm there is no way, from the UI or a direct RPC call with your own session, to delete an Available bottle owned by a different account; the RPC's ownership check makes this fail the same as a nonexistent id.
192. **Mobile layout** — at a 375px viewport, confirm Available rows show both icons with no horizontal overflow or overlap with the wine name, and Reserved/Consumed rows remain just as compact as before this feature.
193. **Existing reserve/return/consume flows are unaffected** — re-run a handful of checks from "Personal Cellar" above (return before tasting starts, mark consumed after reveal) and confirm nothing changed.

### Grape-entry assistance

194. **White form shows only white grapes** — on any bottle-entry form, set Wine style to White; confirm the single-variety dropdown lists only white-skinned grapes plus "Other grape" at the end, with no red-skinned grape (e.g. Pinot Noir) present.
195. **Red form shows only red grapes** — set Wine style to Red; confirm only red-skinned grapes plus "Other grape" appear, with no white-skinned grape (e.g. Chardonnay) present.
196. **Bubbles/Sweet/Other/no style shows every grape** — confirm the dropdown lists both colours plus "Other grape" for each of these.
197. **Burgundy + White auto-selects Chardonnay** — pick France → Burgundy, set style to White with Grape/blend still untouched; confirm it switches to Single variety / Chardonnay and shows "Set from region and appellation. You can edit it." once.
198. **Burgundy + Red auto-selects Pinot Noir** — same, with style Red.
199. **Barolo auto-selects Nebbiolo** — pick Italy → Piedmont → Appellation "Barolo"; confirm Single variety / Nebbiolo is selected automatically.
200. **Bordeaux + Red switches to Blend** — pick France → Bordeaux, style Red; confirm it switches to Blend mode with Cabernet Sauvignon, Merlot, and Cabernet Franc preselected and editable (add/remove a grape, confirm it sticks).
201. **Champagne + Bubbles switches to Blend** — pick France → Champagne, style Bubbles; confirm Blend mode with Chardonnay/Pinot Noir/Pinot Meunier preselected.
202. **Changing a manually-picked grape is never overwritten** — manually choose a specific grape (not the curated match) for a mapped combination, then change an unrelated field (e.g. Vintage) and re-check Country/Region; confirm the manual choice is untouched. Then change Country or Appellation again and confirm it's *still* untouched.
203. **Changing Wine style away from a colour-incompatible manual grape clears it** — manually pick a White grape, then switch style to Red; confirm the grape field clears with "Grape selection cleared because the wine style changed." and does not silently switch to a same-colour grape.
204. **Other grape still works after filtering** — with style set to White or Red, choose "Other grape" and confirm the free-text field appears and behaves exactly as before (validation, multi-variety rejection, persistence).
205. **Draft restore never overwrites a stored grape** — save a guess/bottle with a specific grape, navigate away, and come back (or reload); confirm the stored grape is shown as-is and is not replaced even if the current mapping would suggest something else.
206. **Full blind and Course-by-course guess entry both support assistance** — confirm auto-fill/colour behaviour (now driven by the registered bottle's actual style via the private hint — see "Privacy-safe blind-guess style hint" below) works identically in both guess-entry flows, and that switching between bottles (Previous/Next in full blind, or the next active bottle in course-reveal) starts each bottle's assistance state fresh rather than carrying over the previous bottle's.
207. **Cellar Add and Edit (available bottle) both support assistance** — confirm the same auto-fill and colour-filtering behaviour on `/cellar/new` and an available bottle's edit page.
208. **Scoring, Appellation applicability, and reveal timing are all unaffected** — complete a full tasting using an auto-filled grape/blend; confirm the final report scores it exactly like a manually-entered identical value, with no leaked grape/mapping data before reveal.
209. **Mobile layout** — at a narrow viewport, confirm the filtered dropdown, the inline assistance message, and the style-cleared announcement all render without horizontal overflow and with comfortable touch targets.

### Privacy-safe blind-guess style hint

210. **A blind guess for an actual White wine shows only white grapes** — register a bottle with Wine style White; as a different participant, open its Full blind guess form and confirm the single-grape dropdown lists only white-skinned grapes plus "Other grape", with no red-skinned grape present. Repeat for a Course-by-course active bottle.
211. **A blind guess for an actual Red wine shows only red grapes** — same, with Wine style Red; confirm only red-skinned grapes plus "Other grape".
212. **A blind guess for Bubbles/Sweet/Other shows every grape** — confirm both colours plus "Other grape" appear, with no visible indication of why.
213. **No style/colour information appears anywhere in the visible UI or page source** — inspect the guess form, its HTML/hydration payload, and any status text; confirm nothing states or implies the actual wine's style, colour, actual grape, actual Country/Region/Appellation, or why the dropdown is filtered the way it is.
214. **Bordeaux guessed Region auto-fills differently depending on the hidden actual style** — for an actual Red Bordeaux bottle, guess Country France / Region Bordeaux and confirm the red blend (Cabernet Sauvignon, Merlot, Cabernet Franc) is auto-selected; for an actual White Bordeaux bottle, confirm the white blend (Sauvignon Blanc, Sémillon) is auto-selected instead — with identical, unlabelled feedback text either way.
215. **A manual grape selection is never overwritten** — manually pick a grape in a blind guess, then change the guessed Country/Region/Appellation; confirm the manual pick is untouched.
216. **A saved guess draft restores safely, even if its grape looks "wrong" for the bottle** — save a guess with a specific grape, navigate away, and return (or reload); confirm the exact same grape is shown as selected — never blanked, cleared, or silently replaced — regardless of what the current (hidden) hint would otherwise filter to.
217. **Full blind and Course-by-course guess flows both work end-to-end**, and **Seen tasting, bottle registration, and cellar forms are completely unaffected** by this amendment — re-run a handful of checks from each of those areas.

### Bottle-order contributor labels

218. **Full blind guess entry shows "Bottle N — {name}"** — as a participant in a full_blind session with at least two registered bottles, confirm the progress bar and bottle-code display read e.g. "Bottle 1 — Justin" (using that bottle's actual contributor's display name), and that this updates correctly as you move through Previous/Next.
219. **Course-by-course guess entry shows the same format** — as a participant in a course_reveal session, confirm the active bottle's progress bar and bottle-code display use the identical "Bottle N — {name}" format, and that the locked-guess screen's eyebrow label also shows it after locking in a guess.
220. **Missing contributor name falls back to plain "Bottle N"** — for a bottle with no recorded contributor (e.g. a legacy bottle from before this feature), confirm the label reads just "Bottle N" with no dangling em dash, "Unknown", or "Anonymous", and that navigation/sorting still works normally.
221. **Host-as-participant bottle displays correctly** — if the host also registered a bottle and is tasting as a participant, confirm their own bottle shows their display name exactly like any other contributor's.
222. **Actual wine identity remains hidden pre-reveal** — with the contributor label visible, confirm producer, wine/cuvée, country, region, appellation, grape/blend, vintage, wine style, and bottle photo are still completely absent from the guess-entry UI, page source, and network responses.
223. **Course accessibility constraints are unaffected** — confirm a course-by-course participant still cannot see or guess a bottle that hasn't become the active bottle yet, and that the contributor label never appears for a not-yet-released bottle.
224. **Long contributor names behave safely on mobile** — at a 375px viewport, use a long display name (e.g. "Jean-Baptiste Delacroix-Fontaine") and confirm the label wraps or is contained without causing horizontal overflow or broken layout.
225. **Keyboard navigation and accessible labelling are unaffected** — confirm Previous/Next buttons remain reachable and operable by keyboard, and that focus states remain visible.
226. **Unauthorised access still fails the same way** — confirm an invalid/unknown guest token still returns the existing "your guest session isn't valid" screen, with no contributor name or bottle data leaked in the process.

### Host per-bottle response progress

227. **Full blind: draft does not count, submitted guess does** — as a participant, fill in (but don't submit) a guess; open the host's progress popover for that bottle and confirm the participant still shows under "Still to submit". Submit all guesses; reopen the popover and confirm the count increments and that participant no longer appears.
228. **Full blind: host sees names still to submit** — with several participants and only some submitted, confirm the popover lists exactly the non-submitters' existing display names, one per line, and shows "All eligible participants have submitted." once everyone has.
229. **Course-by-course: unreleased bottle exposes no progress** — confirm the progress icon appears only on the current active bottle's row, not on any other bottle listed in "The table"; confirm a direct request for a non-active bottle's progress is rejected server-side.
230. **Course-by-course: released (active) bottle shows progress** — confirm the active bottle's icon opens a popover with `{submittedCount} of {eligibleCount} submitted` and the correct missing names, matching the existing active-bottle "N of M submitted" text.
231. **Seen: submitted rating increments progress** — as a participant, submit a rating for a bottle; confirm the host's popover count for that bottle increments and that participant drops off the missing list, without needing to reveal the bottle's group rating.
232. **Seen: host sees names still to rate but no values** — confirm the popover never shows an individual rating, the group average, or any score — only counts and missing display names — and that this coexists cleanly with the existing `{ratedCount} of {eligibleCount} rated` line and `Reveal ratings` button.
233. **Host cannot access another session's progress** — using one session's host token, attempt to request progress for a bottle belonging to a different session; confirm it's rejected with the same generic error used elsewhere (no session enumeration).
234. **Participant cannot access host progress** — confirm a guest token cannot call the progress endpoint at all (no valid host-token check ever passes for a guest token).
235. **Open popover updates after a new response** — leave a popover open while a second device submits; confirm the count/names refresh within the existing poll interval without moving keyboard focus or closing the popover.
236. **Mobile and keyboard interaction** — at 375px, confirm the icon remains a ~44px touch target with no horizontal overflow, the popover is fully usable, and Escape/Tab/Enter all work as expected; confirm focus returns to the icon after closing.

### Host guess screen group progress

237. **Host opens Full blind guess and sees group count** — join/create a full_blind session as host, register a bottle, start the tasting, and open the host's own guess-entry page; confirm "Your private guess" and "Group progress · 0 of N submitted" appear above the guess form.
238. **Ordinary participant page has no group count** — as a non-host participant in the same session, confirm neither line appears anywhere on their guess page.
239. **Draft does not increment count** — as a participant, fill in a guess without submitting; confirm the host's group-progress count is unaffected.
240. **Submitted host/participant guess increments count** — submit a participant's guesses (full_blind) or lock a guess (course_reveal); confirm the host's own group-progress count increases without the host reloading their page. Submit the host's own guess and confirm the count increases the same way.
241. **Count updates live** — leave the host's guess page open while another device submits; confirm the count refreshes within the existing realtime/poll interval without moving keyboard focus or reloading the page.
242. **Switching bottle changes count correctly (course_reveal)** — as the host locks their guess and the session moves to the next active bottle, confirm the group-progress line shows the new bottle's own count, never briefly showing the previous bottle's number.
243. **Course mode only shows released bottle count** — confirm the host's own guess screen never shows progress for a bottle other than the current active one, and that a direct request naming a different bottle id is rejected server-side.
244. **No participant names or actual wine identity appear** — confirm the host's own guess screen never lists a submitted or non-submitted participant's name, and never shows producer/wine/country/region/appellation/grape/vintage or any other wine-identity field beyond what the page already showed before this feature.
245. **Host Controls retains its detailed progress popover** — confirm the Host Controls per-bottle popover (see above) is completely unaffected by this feature and still shows non-submitter names exactly as before.
246. **Mobile and keyboard accessibility** — at 375px, confirm the group-progress line reads without horizontal overflow or layout shift, and that it does not interfere with existing Previous/Next/Submit keyboard navigation or focus order.

### Grouped cellar display

247. **Add six matching bottles and see one grouped row** — add six identical bottles via `/cellar/new` (Quantity 6); confirm `/cellar`'s Available tab shows exactly one row reading `Available · 6 bottles`, and the Available tab total reads `Available (6)`.
248. **The same wine shows once in the Add-from-cellar picker** — on `/register/[publicId]/new`, choose "Add from my cellar"; confirm exactly one row reads `6 bottles available`, not six separate rows.
249. **Add one bottle from the picker** — select the group, confirm the one-bottle-group streamlined `Add to tasting` button (no quantity field) is shown only when exactly one bottle remains; with six available, confirm the quantity field appears instead, defaulting to 1; submit quantity 1 and confirm `/cellar` now shows `Available · 5 bottles` and `Reserved for tasting · 1 bottle`, and exactly one tasting bottle was created.
250. **Add two bottles at once** — from a group with several available bottles, set quantity to 2 and submit; confirm the confirmation reads `2 bottles added to tasting.`, exactly two physical bottles moved to Reserved, and exactly two tasting bottles were created, each independently visible/editable during registration.
251. **Filter by Type, Country, Region, and Appellation individually and together** — on `/cellar` and the picker, apply each filter alone and confirm only matching grouped rows remain; combine all four and confirm AND semantics (only rows matching every active filter are shown).
252. **Dependent filters update/reset correctly** — pick a Country, then a Region, then an Appellation; change the Country and confirm Region/Appellation reset to "All ..." only if they're no longer valid for the new Country, otherwise they're preserved; confirm changing Appellation never changes Type/Country/Region.
253. **The same wine in another storage location remains a separate group** — add a bottle identical in every field except Storage location; confirm `/cellar` shows two separate one-bottle groups, never merged into one.
254. **Filtered empty state and Clear filters** — choose a filter combination matching nothing; confirm `No bottles match these filters.` appears with a working `Clear filters` action that restores all four defaults; confirm a genuinely empty tab/view instead shows its existing no-inventory empty state, never the filtered-empty message.
255. **Invalid and stale quantities fail safely** — enter 0, a negative number, a decimal, or a number above the group's available count in "Bottles to add"; confirm the exact `Enter a quantity between 1 and {availableCount}.` error and no request is sent. Reserve/consume/delete one bottle from a group in a second tab, then submit a quantity in the first tab that now exceeds the reduced stock; confirm it fails safely with `The available bottle count changed. Please try again.` and creates no tasting bottles.
256. **Return/consume one bottle and confirm grouped counts/filter options update** — return or consume one bottle from a multi-bottle Reserved group; confirm the group's count decreases correctly (or the row disappears at zero) and, on the picker, filter options no longer offer a now-nonexistent combination.
257. **No other owner's data appears** — signed in as a second account, confirm neither `/cellar` nor the picker's groups, filter options, or counts ever reflect another account's bottles.
258. **Reserved/Consumed groups stay non-editable and non-selectable** — confirm no Edit/Delete ever appears on a Reserved/Consumed group or its expanded rows, and that Reserved/Consumed bottles never appear in the Add-from-cellar picker.
259. **Multi-bottle Available group actions never silently affect every bottle** — expand a group of several Available bottles; confirm each expanded row has its own independent Edit/Delete acting only on that one physical bottle, never on the whole group.
260. **Mobile and keyboard/screen-reader usability** — at 375px, confirm the filter bar stacks with no horizontal overflow, all four selects have visible labels, "Bottles to add" and its validation message are correctly associated, the expand/collapse toggle and "Clear filters" are both keyboard-reachable with visible focus, and no filter change moves keyboard focus unexpectedly.

### Results reveal

261. **Full blind: reveal any bottle in any order** — create a full_blind session with 3+ bottles, submit guesses from two guest devices, then from Host Controls reveal the *last* bottle first; confirm it reveals successfully (no ordering error) and the other bottles remain listed as Unrevealed.
262. **Full blind: confirmation copy matches exactly** — click "Reveal results" on an unrevealed bottle; confirm the dialog reads title `Reveal Bottle {number}?`, body "This will make this bottle's wine details and participant scores available to eligible participants.", and buttons "Reveal results"/"Cancel".
263. **Unrevealed bottle stays private** — before revealing, confirm the participant results hub shows the bottle as "Unrevealed" with no "View results" action, and directly navigating to its result URL shows "Not revealed yet" with no wine identity, score, or guess content anywhere in the page or network responses.
264. **Revealed bottle shows identity + own score only** — as each participant, open the newly revealed bottle's result page; confirm each sees the actual wine identity and only their own score breakdown (never another participant's), and a participant who never guessed sees "No submitted guess for this bottle."
265. **Host sees every participant's breakdown** — open the same bottle's host result page; confirm it lists every eligible participant (submitted or not) with their own field-by-field breakdown, plus the aggregate submitted count/average/highest.
266. **Course-by-course: existing in-order reveal is unchanged** — confirm the host still cannot reveal a bottle out of sequence, and that revealing the current active bottle still works exactly as before.
267. **Course-by-course: host can revisit a revealed bottle** — after revealing bottle 1 and moving to bottle 2, confirm Host Controls' new "Revealed bottles" list still links to bottle 1's results, and they load correctly.
268. **Leaderboard hidden until fully revealed (participant)** — with only some bottles revealed, confirm every participant's results hub shows "The final leaderboard will be available after all results are revealed." with no rank, percentage, or bottle-count leaked.
269. **Provisional leaderboard is host-only and clearly labelled** — as host, open `/host/[publicId]/leaderboard` before every bottle is revealed; confirm it's labelled "Provisional leaderboard — revealed bottles only" and totals only include revealed bottles' scores.
270. **Final leaderboard appears the moment the last bottle is revealed** — reveal the final bottle; confirm the session flips to revealed, the existing `/results/[publicId]` report becomes reachable for every participant, and the participant results hub now links to it instead of showing the "not yet available" message.
271. **Appellation-not-applicable is never penalized or mislabelled** — reveal a bottle whose wine has no recorded Appellation; confirm both the participant's and host's breakdowns omit the Appellation row entirely (never "0/20"), and every score denominator reads "/80".
272. **No cross-session or cross-bottle leakage** — using one session's guest/host token, attempt to load another session's bottle-result or leaderboard URL; confirm a generic not-found/unauthorized response with no confirmation of the other session's existence or contents.
273. **Live updates without a manual refresh** — leave a participant's results hub open while the host reveals a bottle from another device; confirm the newly revealed bottle's "View results" action appears within the existing poll/realtime interval without moving keyboard focus or reloading the page.
274. **Mobile and keyboard/screen-reader usability** — at 375px, confirm the Host Controls per-bottle list, the host per-participant breakdown, and both leaderboard views read without horizontal scrolling; confirm the collapsible rows are keyboard-operable with native `aria-expanded` semantics, and the reveal confirmation dialog traps and restores focus per the existing Modal pattern.

## Automated tests

`npm run test` runs Vitest unit tests covering:

- Text normalisation (`lib/normalize.ts`)
- `legacy_v1` core/bonus field scoring (frozen forever — see README "Scoring model"), grape/blend single-variety and blend-set matching (including order-independence, partial-overlap rejection, mode-mismatch rejection, and legacy-data fallback), NV-vs-year handling, and the 100/20/120-point maximums; `core_v3_appellation_conditional`'s five-category calculation (`calculateBlindScoreV3`), conditional Appellation applicability/denominator (80 vs 100), Appellation correctness independent of every other field, and confirmation Producer/wine-cuvée never affect the score (`lib/scoring.ts`)
- Wine ranking and Wine-of-the-Night tie-breaking, Best Taster via leaderboard rank, Most Divisive Wine, `legacy_v1` taster leaderboard multi-key tie-breaking (total points → core points → exact core matches → shared rank, unchanged), `core_v3_appellation_conditional`'s percentage-primary ranking (excluding bottles never guessed on, tie-breaking on raw points → submitted-guess count → exact core matches), empty-submission states, and the host appearing exactly once in the leaderboard (`lib/results.ts`)
- Country/region reference data: filtering regions by country, the country-change region-reset helper, and grape alias canonicalisation (Syrah/Shiraz, Pinot Gris/Pinot Grigio, Zinfandel/Primitivo) (`lib/wineReferenceData.ts`)
- Structured blend helpers — tokenising/aliasing/deduplicating "other grapes" free text, cross-deduplicating curated picks against free text, alphabetical canonical ordering, and reconstructing curated-picks-vs-free-text from a legacy flattened blend string (`lib/wineReferenceData.ts`)
- Bottle label formatting, the contributor-name bottle-order label (`formatBlindBottleLabel`, including missing-name/whitespace-only fallback and Unicode preservation), and the `registration → collecting → revealed` status order (`lib/codes.ts`, `types/tasting.ts`)
- Bottle form validation — controlled country/region selection, grape/blend mode + value requirements, the two-grapes-minimum blend rule (including cross-deduplication so a curated pick can't be double-counted via free text), the four-digit-year-or-"NV" vintage rule, the required-wine-style rule, and the absence of any price-band requirement (`lib/validation.ts`)
- The guess-entry incomplete-blend check used to block final submission on exactly one blend grape (`lib/validation.ts`)
- Other grape (single-variety custom entry) — the multi-variety-delimiter and valid-custom-name character/length checks and the `isCustomSingleGrape` load-time derivation (`lib/wineReferenceData.ts`); `validateBottleForm`'s Other-grape branch (required/invalid/multi-variety exact error copy, standard-dropdown validation left untouched) and the lenient guess-time `invalidOtherGrapeGuessMessage` check (blank is fine, non-blank-and-invalid blocks final submit) (`lib/validation.ts`); the Other-grape selector/text restoring correctly when loading a bottle, cellar bottle, or guess draft whose stored grape isn't on the curated list (`lib/cellar.ts`, `lib/supabase/guestActions.ts`, `lib/supabase/mappers.ts`); and that a matching custom grape scores correct while a different one scores zero through the existing, unmodified scoring pipeline (`lib/scoring.ts`)
- Seen Host Controls display formatting (`lib/seenHostControls.ts`) — `Producer — Wine/cuvée` primary display; the `Vintage/NV · Appellation, Region · Country` secondary line with missing values omitted cleanly and a region identical to its Appellation never repeated; the exact `{ratedCount} of {eligibleCount} rated` copy; and the exact `Group rating: {average}` / `No ratings submitted` copy, one decimal place
- Blend scoring through the structured picker's derived flattened text — order-independence, alias equivalence, partial-overlap rejection — confirming the existing scoring pipeline needed no changes for this feature (`lib/scoring.ts`)
- Curated Region/Appellation lookups — Burgundy/Chablis, Piedmont/Barolo, unsupported-pair empty results, whitespace/case normalisation, Oregon-not-California Willamette Valley placement, no duplicate values within a pair, and no accidental grape-data overlap; plus form-level appellation validation (optional, membership-checked when non-blank) and the `Appellation, Region, Country` compact-label helper (`lib/appellations.ts`, `lib/validation.ts`)
- The tasting-order move-up/move-down helper — swapping with a neighbour, no-op at either end, no mutation of the input array (`lib/reorder.ts`)
- The wine style constants — the five supported styles and that every one has a display label (`types/tasting.ts`)
- The tasting-mode type guard and the exact required labels/descriptions for all three modes (`lib/validation.ts`, `types/tasting.ts`)
- Building a single revealed bottle's group stats and per-guess score breakdown from the course-reveal RPC's response shape, confirmed to go through the exact same `calculateWineResults` the final report uses (`lib/supabase/mappers.ts`)
- Seen tasting's rating-only pipeline (`lib/seenResults.ts`) — a missing rating shows as `null`/"No rating" rather than a fabricated zero, average/lowest/highest/spread computed only from actual ratings, the four-level bottle-ranking tie-break (average → lower spread → more ratings → shared tie), Wine of the Night and Most Divisive Wine, and that the report never carries any blind-identification/scoring field
- The Tasting Archive's local reference index (`lib/deviceStorage.ts`) — a host/guest token write automatically records an archive reference, references dedupe by `(publicId, role)`, removing a reference never touches the underlying token, corrupt index data is treated as empty rather than throwing, and no raw token is ever serialized into the index
- The Tasting Archive's resolution/authorization logic (`lib/archive.ts`, using fake injected RPC responses — no real Supabase connection needed) — a valid host or guest token on a revealed session produces a "ready" entry with the correct fields; an invalid/rejected token, a session still in registration/collecting, and a guest token that resolves to a session other than the one it was labeled with all correctly produce no visible entry; a session reachable via both a host and a participant reference is shown exactly once, under "Hosted by you"; results are ordered by tasting date then `createdAt` descending; a seen-tasting entry never carries a blind-accuracy figure; the archive summary omits accuracy entirely when no entry has scoring data and never leaks a raw token into a resolved entry; and the request parser enforces the maximum reference-count bound and rejects malformed input
- Account email/OTP-format/display-name validation (`lib/supabase/auth.ts`) — email format acceptance/rejection (including length, whitespace, missing `@`/TLD), OTP format acceptance across the 6-8 digit range a Supabase project can be configured to issue (and rejection outside it), display-name trimming/empty-to-null/max-length truncation, and email masking for the "check your email" step
- The sign-in return-path allowlist (`lib/authRedirects.ts`) — a bare internal path (with or without a query string) is accepted; a protocol-relative URL (`//evil.example.com`), a full external URL, a `javascript:` pseudo-URL, a backslash-based bypass, an embedded scheme separator, embedded whitespace/control characters, a missing leading slash, and an oversized string are all rejected and fall back to the safe default destination
- The account archive's resolution logic (`lib/archive.ts`'s `resolveAccountEntries`, using fake injected session-summary/report responses — no real Supabase connection needed) — a linked, revealed session resolves to an entry; a linked session that's missing or not revealed resolves to nothing, without affecting other records in the same batch; host-role entries never carry a blind-accuracy figure (by design — see "Assumptions and design choices"); participant-role entries compute it from their own `participant_id`; a session linked as both host and participant is deduped to a single host-labeled entry; and results sort by tasting date then `createdAt` descending — reusing the exact same dedupe/sort helpers (`dedupeEntriesPreferringHost`, `sortEntriesReverseChronological`) the browser archive uses, not a parallel implementation
- The account-claim request parser (`lib/archive.ts`'s `parseClaimRequest`) — a well-formed `{publicId, role, token, claimSource}` body is accepted for both `claimSource` values; an invalid role, an invalid `claimSource`, a missing token or publicId, and a non-object body are all rejected
- The Palate Profile's session-link resolution (`lib/profile.ts`'s `resolveSessionLinks`) — a participant-only link resolves via its own guest id; a host-only link resolves via the session's `host_guest_id`; a session with both a host and a participant link is deduped into one `host_and_participant` entry that prefers the participant's guest id for ratings/guesses; two participant links for the same session (joined twice under different names) resolve deterministically to the earliest-claimed identity; a host-only link with no resolvable `host_guest_id` degrades to a null engagement guest rather than throwing
- Best-effort wine identity normalization (`lib/profile.ts`'s `normalizeWineIdentityKey`) — two wines matching on every field after normalization (case/whitespace/punctuation-insensitive) collapse to one identity; differing in even one field never merges
- Wine-observation building (`lib/profile.ts`'s `buildWineObservationsForSession`, fed fake `ReportData` — no real Supabase connection needed) — a standard-mode bottle is only an observation when the engagement guest has a scored guess for it; a seen-mode bottle is only an observation when that guest's rating is non-null; `contributedByYou` is only true when the wine's `contributorGuestId` matches the engagement guest exactly
- At-a-glance aggregation (`computeAtAGlance`) — a missing rating is never averaged as zero; unique wines are counted by normalized identity, not by observation count; first/latest tasting dates come from the deduplicated session list, not per-observation dates
- Blind palate aggregation and its independence from scope (`computeBlindPalate`, `extractBlindObservations`) — core/overall accuracy from earned-vs-possible points across submitted guesses; bottles without a submitted guess excluded from the denominator; the strengths/developing-area readout stays empty below the 10-call/5-per-category thresholds and populates once they're met; Seen-tasting observations are unconditionally excluded regardless of what scope-filtered set the caller passes elsewhere
- Your wine record thresholds (`computeWineRecord`) — "Highest rated" categories require at least 3 rated observations; "Familiar ground" requires at least 2; standout bottles exclude unrated observations; "Most revisited" requires at least 2 distinct sessions, not just 2 observations; "Most explored" ranks by observation count, not unique-wine count
- Recent evenings ordering (`computeRecentEvenings`) — sorted by tasting date descending, capped at 5, and correctly reflects each session's resolved combined role
- Tasted Wines Ledger filtering, sorting, and pagination (`filterLedgerRows`, `sortLedgerRows`, `paginateLedgerRows`) — every filter dimension (country, wine style, minimum rating, contributed-by-you) narrows results correctly; search matches across producer/wine/region/country/grape/session title; sorting by rating (both directions) and recency; an out-of-range page number is bounded to the last valid page rather than returning nothing
- The ledger query parser (`lib/profile.ts`'s `parseLedgerQuery`) — missing/invalid scope, sort, and rating values all fall back to safe defaults rather than trusting client input; valid values pass through unchanged
- Personal Cellar form validation and mapping (`lib/cellar.ts`) — `validateCellarBottleForm` reuses `validateBottleForm`'s exact wine-identity rules and adds only the cellar-only checks (a valid controlled bottle format, a required/length-capped detail when format is "other", and a true-integer Quantity in the 1–100 range that rejects zero/negative/decimal/`NaN`/`Infinity`/above-maximum without ever silently rounding); `cellarBottleFormInputFromRow` maps a raw cellar row back to editable form state, including the same legacy-blend reconstruction fallback as `bottleFormInputFromDto`, and always fixes `quantity` at 1 (never rendered on the edit form); `cellarBottleRpcArgs` nulls out blank storage location/personal note, only sends structured blend components in blend mode, and deliberately never includes quantity (an add-only parameter sent separately — see `lib/supabase/cellarActions.ts` — since `update_cellar_bottle` has no such parameter); `cellarWineIdentityLabel`/`cellarBottleFormatLabel` format display text consistently across the cellar list, selector, and registration confirmation panel
- Cellar deletion eligibility (`lib/cellar.ts`'s `isCellarBottleDeletable`) — `true` only for `available`, `false` for both `reserved` and `consumed`; this is the single source of truth the UI uses to decide whether to render the Edit/Delete icons at all, mirrored (never merely assumed) by `delete_cellar_bottle`'s own server-side status check
- Grape-entry assistance (`lib/wineReferenceData.ts`, `lib/grapeAssistance.ts`) — every curated grape is classified red or white, and `singleGrapeVarietyOptionsForStyle`/`isGrapeColorCompatibleWithStyle` filter/clear correctly for White, Red, and every either-colour style, including that an unrecognised (custom "Other grape") value is always treated as compatible; `getGrapeAssistance`'s four-tier precedence (Country+Region+Appellation+Style → +Appellation → +Style → Country+Region, else null) resolves correctly including a conflicting-style appellation match beating a same-style region fallback, a style-only mismatch falling through to a broader tier rather than returning null, no country-only mapping, the Oregon/California Willamette Valley distinction, and the deliberately-unmapped Paso Robles White/Alsace/Pfalz/Wachau/Riverina/Menfi cases; every mapped `single` result has exactly one grape and every `blend` has at least two, all drawn from the real standard grape list (enforced additionally by dev-time validation that throws at import time if ever violated); and the pure `evaluateGrapeAssistanceChange` state machine — empty-field auto-apply (single and blend), recomputation on a further change (including switching kind as the match itself changes), clearing when a mapping disappears, manual values never overwritten by a location/style change, and the Part 1 colour-incompatibility clear taking precedence over manual preservation without also applying a fresh mapping in the same event
- Wine-identity form docs cross-check: no test exercises React hooks directly (this codebase has no component-test harness — see "These don't require a Supabase project" below); `lib/useGrapeAssistance.ts` is a thin, directly-inspectable wrapper with all its actual decision logic in the pure, fully-tested `evaluateGrapeAssistanceChange`
- Blind-guess privacy-safe style hint (`lib/grapeAssistance.ts`'s `styleFilterKeyForHint`) — `white_skin_only`/`red_skin_only`/`all_skins` map to the exact same `"white"`/`"red"`/`""` vocabulary `getGrapeAssistance`/`singleGrapeVarietyOptionsForStyle` already accept, confirmed by feeding each through to a real mapping lookup (a Bordeaux guess resolves to the red blend under a Red-derived key and the white blend under a White-derived key; a Burgundy guess resolves to Chardonnay/Pinot Noir the same way); a manual guess grape is preserved across a location change even with a fixed hint in place; and neither of the two messages reachable inside a blind-guess form ever mentions style/colour. `lib/wineReferenceData.ts`'s `singleGrapeVarietyOptionsPreservingCurrent` — behaves identically to the plain filter when the current value is blank or already compatible, prepends (never drops or duplicates) an incompatible current value instead of silently excluding it, and ignores an unrecognised (custom Other-grape) current value rather than inventing an entry for it
- Host per-bottle response-progress formatting (`lib/hostProgress.ts`) — the exact required `Bottle {number} guess progress` title, the fixed `Rating progress` Seen title, the context-specific accessible label per response kind, the `View guess/rating progress` tooltip text, the exact `{submittedCount} of {eligibleCount} submitted/rated` status line, the `Still to submit`/`Still to rate` heading, the `All eligible participants have submitted/rated.` all-complete copy, the `No eligible participants yet.` zero-eligible copy, and the polite live-region update announcement text; plus the host guess screen's own `Group progress · {submittedCount} of {eligibleCount} submitted` line and its `Group progress updated: …` announcement text, confirmed to reuse the same underlying status-line formatter and to never contain the word "guests"
- Grouped cellar display (`lib/cellarGrouping.ts`) — identical bottles group with the correct count; every material identity field (status, location, format, vintage/NV, producer, wine/cuvée, country, region, appellation, grape/blend mode and text, structured blend components, wine style, custom Other-grape text) independently prevents grouping when it differs, including single-variety vs. blend and materially different blend component lists; differently-ordered but otherwise-identical blend picks still group; grouping normalization is whitespace/case-only, never accent-stripping or other fuzzy matching; personal notes are excluded from the key and every note is preserved unchanged across a group; the oldest-`created_at`-then-`id` deterministic ordering helper; and the `1 bottle`/`{n} bottles`/`1 bottle available`/`{n} bottles available` grammar and per-status group summary line text
- Cascading cellar filters (`lib/cellarFilters.ts`) — Type options reflect only canonical styles actually present, in canonical order; Country/Region/Appellation options cascade correctly from their parents and never include a blank/null value; ambiguous Region/Appellation labels gain a disambiguating suffix while their filter *value* stays the plain string; `applyCellarFilters`'s AND semantics across all four dimensions; `updateCellarFilter`'s cascading resets (a dependent filter is kept only if still valid for the new parent values, otherwise reset to "All ...", and changing Appellation never touches its parents); and `hasActiveCellarFilters`
- Results reveal (`lib/resultsReveal.ts`) — `buildHostBottleResult` scores every eligible participant through the exact same `scoreWineGuess` the rest of the app uses (never a duplicated formula), never fabricates a score for a participant with no submitted guess, uses the bottle's own uniform denominator (80 or 100, dropping to 80 with no penalty when Appellation doesn't apply), and computes average/highest/perfect-score-count only across actually-submitted guesses; `formatBottleAggregateSummary`'s exact `"No submitted guesses to score yet."` empty-state copy and `"Average score: X / Y"` populated copy; `buildProvisionalLeaderboard` ranks by percentage accuracy using only the revealed-bottle data it was handed, correctly excludes a full_blind guest with no session-wide `completedAt` and a course_reveal guess with no `lockedAt`, breaks a genuine tie alphabetically by display name while preserving a shared rank, and reports `allRevealed` only once every bottle in the session has actually been revealed

These don't require a Supabase project — they test the pure scoring/ranking/validation/authorization-classification logic in isolation. Bottle-numbering concurrency, RLS enforcement, the registration/collecting/revealed lifecycle transitions, the live Supabase Auth OTP send/verify round trip, the `claim_account_tasting_record` RPC's own token/revealed-status validation, the `/api/profile`/`/api/profile/ledger` Route Handlers' end-to-end authorization (RLS-scoped `account_tasting_records` reads, the new `host_guest_id` grant, the `profiles.include_seen_tastings` column-grant write), the Personal Cellar RPCs' own transactional/concurrency guarantees (atomic reserve + tasting-bottle creation, double-reservation prevention via row locking, cellar-aware `delete_bottle`, return/consume eligibility checks, and `delete_cellar_bottle`'s conditional-delete enforcement), `wine_style_grape_options_hint`'s own SQL-level derivation from a bottle's actual `wine_style` (verified the hint never leaks the raw style and is only ever returned within the caller's own token-scoped bottles), `get_bottle_response_progress`'s own SQL-level eligibility/submission counting and host-token/active-bottle authorization (verified it never returns a guess, a rating, or a score, and rejects a non-active course-mode bottle), `get_host_guess_progress`'s own SQL-level guest-token-to-host verification and active-bottle authorization (verified it returns only the two counts, rejects a non-host guest token and a non-active course-mode bottle, and is a wholly separate DTO from `get_bottle_response_progress` with no participant names), `register_bottles_from_cellar_group`'s own SQL-level group recomputation, row locking, and all-or-nothing transaction (verified it never trusts a client-supplied bottle set, reserves exactly the requested quantity of currently-available matching rows or none at all, and can never double-reserve a bottle under concurrent calls), and Results reveal's RPCs (`reveal_full_blind_bottle`'s any-order idempotent reveal and session-status auto-flip on the last bottle; the tightened `get_revealed_bottle`'s own SQL-level narrowing to the caller's own guess only and its mode-aware submitted predicate; `get_bottle_result_for_host`/`get_provisional_leaderboard_for_host`'s own host-token verification and hard gate on `revealed_at is not null`, verified to never return an unrevealed bottle's identity or guesses under any host-token-valid request; and `get_revealed_bottles_summary`'s own guest-token verification) are database- or live-API-level guarantees exercised by the manual test checklist above against a live project, not by this unit-test suite — consistent with how every other RPC-backed workflow in this app has always been verified.
