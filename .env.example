NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# No additional env vars are needed for Supabase Auth (email OTP sign-in) —
# see SUPABASE_SETUP.md "Setting up Supabase Auth (accounts)" for the
# Dashboard-only settings (Site URL, email template) that flow does need.
